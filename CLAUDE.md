# 作業指示(常時適用)

- 応答・作業ログはすべて日本語で出力すること。
- ビルドエラー、テスト失敗の詳細、T-state計測結果など、デバッグに役立つ情報は省略せず日本語で説明しながら進めること。
- 作業再開時は `tools/stage2_combined/HANDOFF.md` の末尾(最新Round)を必ず読むこと。
- **このCLAUDE.mdは強い指示として扱う。作業の進捗・方針・保留タスクなどに変更があった場合は、
  その都度このファイルも適宜更新すること(放置して古い情報のまま残さない)。**

## プロジェクト構造(探索不要)

- 開発中の作業ファイル: `tools/stage2_combined/combined_test.asm`
- 実際に出荷されるゲーム本体: `src/CYBER SHMUP.asm`(Stage1)
- Round単位の作業履歴・技術的決定・ユーザー発言の逐語記録: `tools/stage2_combined/HANDOFF.md`
  - 177KB超の大きいファイル。全文Readせず、末尾(最新Round)だけを見ること。
    例: `tail -c 8000 tools/stage2_combined/HANDOFF.md` や `grep -n "^## Round" ... | tail`
- `tools/stage2_combined/README.md` は360KB超(設計メモの蓄積)。全文Readしない。目次代わりに
  `grep -n "^# \|^## "` で見出しだけ拾ってから該当箇所を読むこと。

## ビルドコマンド

- Stage2テストROM: `cd tools/stage2_combined && python3 build_test.py`
  → 出力: `tools/stage2_combined/CyberS S2.ascii16k.rom`
  → 実測所要時間: **0.2秒**(アセンブル自体はボトルネックではない)
- Stage1+実Stage2 統合ROM("Comb"ビルド): `cd tools/bankswitch_poc && python3 build_full_rom.py`
  → 出力: `rom/CyberS Comb.ascii16k.rom`(旧`CyberS S1.ascii16k.rom`は廃止・git管理からも削除済み)
  → 実測所要時間: **0.5秒**
  → bank2/bank3は2026-08-25より`tools/stage2_combined`の実コンテンツ(地形・戦車・敵・Sasapiボス戦)。
    詳細は`tools/bankswitch_poc/README.md`の「Full-game integration test」参照。
    検証: `cd tools/bankswitch_poc && python3 verify_comb.py`(エミュレータでStage1→実Stage2への
    バンク切替を一通り確認、バンクインデックス退避のバグを主眼に検証)。
  - **(2026-08-29、ユーザー指示、2026-09-01付けで上書き・撤回済み)** "まずCombは指示がない限り
    いらない" - Stage2側の変更のたびにCombを自動でビルド・送付しないこと、という当初方針だった。
  - **(2026-09-01、ユーザー指示で上記を上書き) "Romくれ 確認のために毎回Romは出すように"** -
    **以後、Stage1・Stage2いずれの変更であっても、コード変更を伴う作業が一区切りついたら
    (コミット時点を目安に)毎回Comb ROMをビルド・`verify_comb.py`で検証した上で、ユーザーへの
    確認を求めるまでもなく毎回自動的に送付すること。** 明示的な要求を待たない。この指示は
    上記2026-08-29の指示を明確に上書きするもの。
  - **(2026-09-06、ユーザー指示で上記を部分的に上書き・現在有効) "あとタイトルのみのROMは
    もういらない タイトルBGMも停止 Stage2も当面不要 まともになるまでCombのみで"** -
    BGM周り(SEショット優先・HWエンベロープ)がまだ安定しない現状を踏まえたスコープ縮小指示。
    **以後、状況が「まとも」になったとユーザーから明示されるまで:**
    - **Title単体ROM(`tools/title_screen/CyberS Title.ascii16k.rom`)・Stage2単体ROM
      (`tools/stage2_combined/CyberS S2.ascii16k.rom`)はビルド・送付しないこと**
      (アセンブル自体は開発中の動作確認のために引き続き行ってよい、あくまで**ユーザーへの
      送付**を止める指示)。
    - **送付するROMはComb ROMのみ**。上記2026-09-01の「Comb ROMを毎回自動送付」指示自体は
      継続有効。
    - タイトル画面自身のBGM再生を停止(`tools/title_screen/title_test.asm`のINIT_BGMから
      HTIMI_HOOK設置を削除、RAMコピー[Stage1が読む分]自体は維持)。詳細はHANDOFF.mdの
      Round42(タイトルBGM停止+送付スコープ縮小)参照。

ビルド(アセンブル)そのものは高速。「ビルドが遅い」と感じる場合、実際は次項の
回帰テスト(Z80エミュレータでの命令実行)が重い。オブジェクトファイル分離・リンカ機能の
実装は効果がほぼ無いため不要と判断済み(2026-08-25 実測により確認)。

## テストコマンド・実行方針

- 全回帰テスト: `cd tools/stage2_combined/tests && python3 run_all.py`
  - **実測所要時間: 約35秒**(2026-08-29時点921テスト。テスト数は変更のたびに増減する、
    629は2026-08-25の高速化計測当時の件数。2026-08-25の高速化前は約20分だった - 経緯は下記)。
  - この所要時間ならフォアグラウンドで待っても問題ないレベルだが、環境によりPyPyが
    無い(セッション開始hookが未実行/失敗した)場合は数分かかることもあるので、
    念のため引き続きバックグラウンド実行(`run_in_background`)を推奨。
  - 変更内容が特定の機能に限定される場合は、対応する個別テスト(例: `dash_test.py`)
    だけを先に実行して素早く確認し、全回帰は最終確認時にまとめて回す。
  - 個別テストは同ディレクトリの `*_test.py` を直接実行(`python3 xxx_test.py`または
    `pypy3 xxx_test.py`、後者の方が速い)。

### 高速化の経緯(2026-08-25、629 passed/0 failedを維持したまま約34倍)

ボトルネックはアセンブルではなく`z80emu.py`(Pure Python製Z80命令エミュレータ)による命令
ステップ実行(`boss_test.py`のプロファイルで、`cpu.step()`だけで244秒中96.7秒)。実施済み:

1. **`run_all.py`の並列実行**(`tests/run_all.py`): 各テストファイルは完全に独立したsubprocessな
   ので、`ThreadPoolExecutor`(`os.cpu_count()`ワーカー)で並列起動。19分53秒→8分23秒。
2. **`fresh_cpu()`のブート状態キャッシュ**(`tests/banked_helpers.py`): 同一プロセス内で
   ブートトレースは1回だけ実際に実行し、以降は`copy.deepcopy()`したスナップショットを返す。
   `fresh_cpu()`を多用するテストに効くが、`boss_test.py`自体は最重量テストケースが
   `step_frame()`を8000回連続実行する形なので、この施策だけでは効果は限定的だった。
3. **`z80emu.py`の`Z80.step()`オペコードディスパッチ並べ替え**: 実際のオペコード出現頻度を
   計測し(`boss_test.py`実行中に42.6M回の`step()`呼び出しをカウント)、if/elif連鎖の中で
   出現頻度の高い分岐(LD r,r'ブロック~8.8M、DDプレフィックス~4.8M、LD A,(nn)~3.7M、
   DJNZ~2.2M等)を先頭付近に移動。全64分岐は`op`値で完全に排他的条件かつ条件式に副作用が
   無いため、並べ替えは動作を一切変えない(新旧ファイルの全64ブロックを集合として比較し、
   内容が同一であることを機械的に検証済み)。8分23秒→6分8秒。
4. **PyPy導入**: 単体テスト(boss_test.py)で57秒→5.4秒(約10.6倍)、全回帰で6分8秒→34.7秒。
   `run_all.py`は`shutil.which("pypy3")`があれば自動的にそちらでテストサブプロセスを起動する
   (無ければ従来通り`sys.executable`にフォールバック、ポータブル)。このセッションのコンテナは
   使い捨てだが、`.claude/hooks/session-start.sh`(`.claude/settings.json`のSessionStart hookで
   登録済み)が`CLAUDE_CODE_REMOTE=true`の時に`pypy3`が無ければ`apt install`するので、
   このリポジトリのdefault branchにマージされていれば以後のweb/リモートセッションでも
   自動的にPyPyが使えるようになる(ユーザーへの確認を得て2026-08-25導入・登録済み)。

オブジェクトファイル・リンカ分離は見送り済み(アセンブル自体は0.2〜0.5秒でボトルネックでは
ないため効果なしと判断、上記「ビルドコマンド」の項参照)。

## 環境依存パスに関する注意(重要)

- このセッションの実ディレクトリは `/home/user/MSX-STG`(大文字)。過去のセッションでは
  `/home/user/msx-stg`(小文字)だったことがあり、**テストコード等に環境依存の絶対パスを
  ハードコードしないこと**。`__file__`からの相対パスを使う(`build_test.py`の
  `REPO = os.path.join(HERE, "..", "..")` パターンが正しい例)。
  - 2026-08-25: `tests/terrain_render_perf_test.py` に `/home/user/msx-stg/tools` への
    ハードコード絶対パスが残っており、このセッションの実ディレクトリ(大文字)と食い違って
    `ModuleNotFoundError: No module named 'mini_z80asm'` でクラッシュしていたのを発見・修正済み。
    全回帰テストの合計が629ではなく626+クラッシュ1件と表示され、テストカバレッジが
    静かに欠落していた(SKIPリストにも入っておらず、失敗ともカウントされない状態だった)。

## 保留中タスク(指示なしに着手しない)

- (2026-08-25、着手・完了済み) "Comb"ビルド: `build_full_rom.py`のstage2部分を、`bankswitch_poc`の
  簡易プレースホルダーから本物の`stage2_combined`contentに差し替え済み。詳細は上記「ビルドコマンド」
  および`tools/stage2_combined/HANDOFF.md`のRound30を参照。
- **Stage2の敵スポーンをStage1同様スケジュールテーブル駆動にする件**: 完了済み(2026-08-29)。
  - 第一段階(2026-08-29): `tools/schedule-editor.html`をStage2エネミー(ZacoII/Zum/BigZum/
    Flyer/Etank/Boss)対応に拡張。Stage1/Stage2切り替えボタン、相互登録禁止(`s2_`プレフィックスの
    名前空間分離)、出力ファイル分離(`stage`フィールド・`Schedule.json`/`Schedule2.json`)を実装。
    詳細は`tools/stage2_combined/HANDOFF.md`のRound33を参照。
  - 第二段階(2026-08-29、Round34): `tools/stage2_combined/combined_test.asm`側を実装。
    ユーザーがschedule-editorで実際に作成した152件のスケジュールJSONを、Stage1の
    `SPAWN_THRESHOLDS`/`SSC_FIRE`/`GAME_TICK`方式に倣ってテーブル駆動化
    (`SPAWN2_THRESHOLDS`/`SPAWN2_NEXT_INDEX`/`SSC2_FIRE`/`SPAWN2_SCHEDULE_CHECK`)。
    旧来のランダムスポーン(個別インターバルタイマー・ランダムY・ランダムバリアント・
    `ENEMY_SPAWN_STOP_TICK`一律ゲート)は全廃止。
  - 第三段階(2026-08-29、Round34-2、実機フィードバック対応): 地上敵(Zum/BigZum/Etank)の
    相互排他制御を削除(明示指示。BigZum⇔Etankはパターンvram間借りのため理論上は同時生存で
    見た目破損リスクあり、ASMコメントに明記済み・現スケジュールでは実害なしを確認済み)。
    GAME_TICK表示(`GAME_TICK_DISPLAY`)がMOD1000で"000"に折り返し、ユーザーに「ゲームが
    再開したように見える」バグを修正(実カウンタ自体は無制限に増加継続、表示のみ999で
    クランプ - カウンタ自体を止める最初の実装はボスの内部タイマーを壊す新規バグだったため
    自己発見・修正した経緯あり)。**この段階で追加した`SPAWN2_STALL_LIMIT`(ブロックされた
    エントリを60Tickリトライしてから強制スキップする安全弁)自体が、次段階で判明する
    重大バグの原因だった(下記参照)。**詳細・技術的経緯は`tools/stage2_combined/HANDOFF.md`の
    Round34-2を参照。
  - 第四段階(2026-08-29、Round34-3、実機フィードバック対応・完了済み): "Tick500あたりから
    100Tick以上敵が出てこない/Bigzumが一度も出てこない/ボスも999になっても出ない/以前の
    1300あたりで変わってない/やってることはStage1と全く同じ処理だぞ"という指摘を受け、
    根本原因が`SPAWN2_STALL_LIMIT`の「retry-until-success」方式そのものだったと判明
    (地形の平坦待ちが60Tickを超えることが普通にあり、BigZumがほぼ毎回強制スキップされ、
    その間`SPAWN2_NEXT_INDEX`が固定され後続エントリも道連れで止まっていた)。`SSC2_FIRE`を
    Stage1の実際の`SSC_FIRE`と完全に同型(`SPAWN2_NEXT_INDEX`を毎回無条件に先へ進めてから
    ディスパッチ、失敗時はリトライせず単にdrop)に書き換えて解消。`SPAWN2_STALL_LIMIT`/
    `SPAWN2_STALL_COUNT`/`SSC2_ADVANCE`は全廃止。エミュレータでの実測により、BigZumが
    実際にスポーンすること、tick500付近の空白が解消したこと、ボスがtick995(理論上最速)
    ちょうどでスポーンすることを確認済み。詳細・技術的経緯は`tools/stage2_combined/
    HANDOFF.md`のRound34-3を参照。**ただしこの段階の「BigZumが実際にスポーンする」は
    「以前はゼロ回だったのが1回は成功するようになった」という意味に過ぎず、まだ
    "4回以上スケジュールしているのに1回しか出ない"という状態自体は解消していなかった
    (真因は次段階で判明)。**
  - 第五段階(2026-08-29、Round35、実機フィードバック対応・完了済み): "Bigzumは4回以上
    スケジュールしてるが1回しか出てない...恐らくEtankでスキップされてるな...お前もしかして
    排他制御をエネミーにハードコードしたな"という指摘を受けて調査。**結論:
    Etank排他制御は本当に存在しなかった**(コード直接確認・エミュレータ計装の両方で
    立証、ただし1箇所だけ現状と矛盾する古い解説コメントが残っていたため修正)。真因は
    `BIGZUM_SLOT_COUNT=1`の唯一のスロットを、一度スポーンした個体が自然には消滅せず
    `BIGZUM_RETREAT_TICK=950`という単一グローバル基準まで居座り続けていたこと
    (BigZum自身による自己ブロック)。撤退判定をインスタンス単位
    (`min(スポーンtick+BIGZUM_ENGAGEMENT_DURATION, 950)`)に変更して解消。続けて
    "スポーン条件も要らないぞ 地形も仮実装だから平地条件いらない"という指示を受け、
    `ZUM_TERRAIN_OK`/`BIGZUM_TERRAIN_OK`/`ETANK_TERRAIN_OK`(地形の平坦チェックによる
    スポーンゲート)を3種とも完全撤廃。Flyerのスロット数も1→2に拡張済み(hwスプライト
    スロット配置・RAM配置の詳細な調整を伴う)。エミュレータでの実測により、BigZumの
    スポーン成功率が1/6→5/6に改善(残り1件はスケジュール上13Tick間隔という物理的に
    間に合わない配置が原因、コード側の不具合ではない)したこと、Flyer同時2体稼働、
    ボスのタイミング・VRAM安全性(4プール全て空でボス出現)が変わらず保たれていることを
    確認済み。全回帰928 passed/0 failed。詳細・技術的経緯は`tools/stage2_combined/
    HANDOFF.md`のRound35を参照。

## 地形もエディット対象に(2026-08-29〜、Round36)

- ユーザー指示: "地形もエディット対象に 現在の地形データをJsonに含めて出力してくれ
  で、スケジュールエディタの地形エディット対応"。GAME_TICKと地形スクロール位置
  (PXCHAR_T)がMAINLOOP内で完全に同期していること(同一の「8フレームに1回」ゲート)を
  突き止め、スケジュールエディタの既存グリッド(row20-23が実際の地形描画行と一致)に
  そのまま地形をペイントできる設計とした(ユーザーにAskUserQuestionで編集方式を確認、
  「既存グリッドに直接ペイント」を選択)。
- `tools/stage2_terrain/terrain_gen.py`をデータ駆動化: `build_track()`をハードコード
  呼び出し列から`(tier,run)`のプロファイルリストを歩く形に**出力を完全に変えずに**
  リファクタ(直接diffで新旧一致を検証済み)。列ごとのtier配列⇔プロファイルの相互変換
  関数、JSON出力(`export_terrain_json()`)を追加。
- `tools/schedule-editor.html`に地形ペイントツールを追加(当初はTerrainトグル
  ボタン+専用ペイントモード、Stage2のみ表示)。row20-23をクリック/ドラッグで
  地表の高さ(tier0-3)を直接編集、Undo統合、JSON保存/読込に`terrain`フィールドを
  追加。Playwrightでの直接検証により発見・修正した不具合(配列転記ミス・高速
  ドラッグでの塗り漏れ)込みで完了。
- **(2026-08-29、Round36-2、実機フィードバック対応)**: "まずJsonロードできない
  Stage1も読み込み出来なくなった で、地形の変なボタンはいらない 敵と同じで
  アイコンでいい 敵アイコンの隣に並べればよい で、その際は4行より上には置けない
  ように制限"という指摘を受け、専用Terrainボタン・専用ペイントモード・ドラッグ
  ブラシを全廃止。代わりにパレット(Stage2の敵一覧)へ`s2_terrain`という通常の
  アイコンを追加し、敵と全く同じ「パレットからarm→グリッドタップで配置」という
  操作方式に統一(1タップ=1列、ドラッグでの連続塗りは廃止)。配置行はrow20-23
  以外は拒否(place不可)する制限を実装。JSONロード不具合はPlaywrightでの
  直接再現テストではコード自体の不具合を再現できず、`Terrain2.json`(単独では
  `placements`キーを持たない)をそのまま読み込ませようとした可能性が高いと
  判断 - `importJSONText()`側も`placements`配列が無い/不正な場合はエラーに
  せず0件として扱うよう耐性を追加した。
- **今回はスコープ外(意図的)**: `build_test.py`を`Terrain2.json`から実際にASMの
  地形テーブルを生成するように配線する作業は未実施。Schedule2.jsonの前例
  (エディタ対応→ユーザー編集→ASM実装、の2段階)に倣い、ユーザーが実際に地形を
  編集した結果を見てから対応する。
- **(2026-08-29、Round36-3→36-4で撤回済み)**: Round36-3で「地形選択アイコンが
  どこにあるか分からない」「グリッド最下行が見えない」への対応として、
  「コンソールのアーティファクト(サンドボックス化されたiframe)で`<input
  type="file">`が機能していない」という仮説のもと「Load Text」(JSON直接
  貼り付け読込)ボタンを追加したが、**この仮説はRound36-4でユーザー提供の
  実機スクリーンショット(Android実機、`content://`で生のローカルファイルを
  実ブラウザで開いていた)により誤りと判明**。真因は単純にRound36/36-3で
  サイドバーへ追加した新規ボタン行(Terrain・Load Text)がスマホ幅の縦積み
  レイアウトでグリッドの可視高さを圧迫していたことだった。**Load Text機能は
  完全に撤回し、サイドバーのボタン行構成をRound36着手前のコミットと
  byte-for-byte一致するところまで復元した**(git diffで確認済み)。地形選択は
  引き続きパレット内のアイコン1個としてのみ存在し、新規ボタン行は追加しない
  (パレットは元々`overflow-y:auto`でスクロール可能なため、アイコン1個の追加は
  ボタン行追加とは異なりサイドバー高さに影響しない)。**この一連の顛末(局所
  対応→レイアウト再破壊→撤回)を踏まえ、以後サイドバーに新規ボタン/ボタン行を
  追加する変更は、明示的な指示がない限り行わないこと。**
- **(2026-08-29、Round36-5、実機フィードバック対応)**: "エディタ自身に地形
  埋め込んでんじゃねえ...汎用性や自由度下がるだろうが"という指摘を受け、
  Round36で追加した`DEFAULT_TERRAIN2`(現在の地形492列をJS配列として
  schedule-editor.html自体に埋め込んでいたもの)を完全に削除。エディタ自身は
  `placements`同様、特定のゲーム状態を一切前提としない汎用ツールに戻し
  (`terrain2`の初期値は全列tier0のフラットな空白キャンバス)、「現在
  実装してある敵+現在の地形」は別途1つのJSONファイルとして直接送付する形に
  統一(Schedule2.json/Terrain2.jsonと同じくリポジトリには非コミット)。
  **エディタ自身に何らかのゲーム固有データを埋め込むことは、明示的な指示が
  ない限り今後も行わないこと。**
- 詳細・技術的経緯は`tools/stage2_combined/HANDOFF.md`のRound36/Round36-2/
  Round36-3/Round36-4/Round36-5を参照。
- **(2026-08-30、Round36-6、実機フィードバック対応・完了済み)**: "お前のスクショでも
  Row23が見切れてんじゃねえかよ これじゃ23がタップできず 1段の地形が指定出来ねえ
  だろうが"という指摘を受け、`layout()`内の`availH`計算が`.scrub-row`(スクラブ
  スライダー行)の高さを一切差し引いておらず、グリッドキャンバスが実際に確保できる
  高さより約1行分(≒32px)大きく描画され続けていたバグを修正(Row23が常に画面外に
  クリップされ、地形tier0の指定が不可能だった)。`.scrub-row`は実測
  (`offsetHeight`)、`.ruler-row`は引き続き`RULER_H`定数を使用(実測すると初回
  呼び出し時に`#ruler`キャンバス未リサイズによる循環依存で別の不具合が出るため)。
  デスクトップ幅・モバイル幅の両方でPlaywright実測・Row23への実際のタップ操作
  (`terrain2`へのtier0設定)を確認済み。
- **(2026-08-30、Round36-7、実機フィードバック対応・完了済み)**: "こんな地形はない
  実行時とエディタの見た目が一致してなきゃエディット出来ねえだろうが Rock225も
  きちんと反映させろ 自動描画や見なし処理すんな"という指摘を受け、地形描画を
  改善。ユーザーへの確認("斜面をtick軸を保ったまま既存カラム境界に描くだけか/
  実際に2カラム挿入して実コンパイル後の長さも一致させるか")には"エディタの
  描画の問題だけで 本編に影響はないはずだ"との回答 - よってterrain2のデータ
  形式・列数・tick対応は一切変更せず、Canvas描画のみ変更する方針に確定。
  (1)「まだRockが生えていない帯」を半透明でほぼ見えない色(疑似的な空)から
  実際に視認できる砂色(`--terrain-sand: #a68a4a`)に変更、(2)tier変化点を
  垂直な階段(直角段差)ではなく、変化が起きる1列分の幅に収めた斜め線
  (Rock225の登り/下り斜面に近い見た目)で描画するよう変更。デスクトップ幅・
  モバイル幅の両方で実データ(`Schedule2_current.json`)を読み込んで確認済み。
  なお実機で報告された「チェッカーボード状ノイズ」はこの修正後も含め一貫して
  再現できておらず、実機側の古いキャッシュ由来である可能性が高い(未確定)。
  実際のコンパイル済みトラックへの正確な反映(`build_test.py`のTerrain2.json
  配線、2カラム挿入によるTRACK_LEN自体の伸長)は、今回のユーザー回答により
  明確にスコープ外と確定(指示なしに着手しない)。
- **(2026-08-30、Round36-8、実機フィードバック対応・完了済み)**: "描画だけで
  いい いきなり4段分あがる見た目では見て判断が難しい"という指摘を受け、
  Round36-7の斜面描画(1段の変化を1列分の幅に収めて斜めに描く方式)が2段
  以上の急な変化(本来の実ゲームでは起こり得ないが、エディタ自体は自由に
  ペイントできてしまう)の場合ほぼ垂直な壁にしか見えなかった問題を修正。
  生のterrain2値を直接使わず「1列あたり最大1tierしか動けない」スルーレート
  制限フィルタ(`smoothTier`)を描画直前にかけることで、1段の遷移は従来通り
  1列で完結、2段・3段の変化はそのぶん複数列にわたる緩やかな斜面として
  描かれるようにした。スクロール位置に依存せず同じ見た目になるよう毎回
  tick0から再計算する設計。データ形式・列数は今回も変更していない。
- **保留中(指示なしに着手しない)**: `BIGZUM_ENGAGEMENT_DURATION=100`は未調整の初期値
  (実プレイでの難易度・ペーシング調整はスケジュール自体の調整と合わせて引き続き保留)。
  Sasapiボスの実物64x64ボディをschedule-editor.htmlに転写する件(Round33で明示的に
  スコープ外とした通り、引き続き未着手)。「ボスが終わったら終わり」の明示的な
  ステージクリア画面等の新規UI実装(現状は新規スポーンが構造的に発生しなくなるだけで、
  明示的な終了演出は未実装)。Etankが地形非依存でスポーンするようになったことで
  プレースホルダー地形の上下に浮いて見える可能性(実害未確認、実機報告待ち)。
- **(2026-08-30、Round36-9、完了済み)**: "これで組み込んでみてくれ"という指示で
  ユーザー自身がschedule-editor.htmlで編集した`Schedule2_7.json`(placements150件+
  terrain492列)を実際に本編(`tools/stage2_combined/combined_test.asm`)へ統合。
  上記の「地形編集の実ゲームへの反映」保留はこれで解消。
  - `terrain`配列に1箇所(列index36、tier3→1の物理的に不可能な2段ジャンプ)を発見。
    ユーザーへの確認は行わず(ビルド自体をブロックする技術的制約 - `terrain_gen.py`の
    `columns_to_tier_profile`がこの種のジャンプに対しassertで即座に失敗するため)、
    最小限の訂正(3→2→1、index36を1から2に変更)を明示的に適用の上で統合。
    詳細はHANDOFF.md Round36-9参照、ユーザーは結果を見て別の意図だった場合は
    訂正可能。
  - `terrain_gen.py`の`DEFAULT_TIER_PROFILE`を、上記terrain配列からRLE変換した
    新プロファイル(21要素)に置き換え。`TERRAIN_TRACK_LEN`は516→532に変化
    (遷移が増えたため自動的に伸長、想定通りの挙動)。
  - `combined_test.asm`の`SPAWN2_COUNT`(152→150)/`SPAWN2_THRESHOLDS`/
    `SPAWN2_Y_TABLE`/`SSC2_FIRE`のCPディスパッチチェーンを、新JSONから機械的に
    再生成したテキストで丸ごと置換(手作業transcriptionではなくPythonスクリプトで
    生成、`tools/schedule-editor.html`のtype→ハンドラ対応表を直接コード化)。
  - `python3 build_test.py`でアセンブル成功、全回帰テスト926 passed/0 failed
    (該当する`spawn2_schedule_test.py`161件含む)を確認済み。
- **(2026-08-30、Round36-10、実機フィードバック対応・完了済み)**: "Rock225の反転の
  下り描画の横に自機ショットらしきゴミが描画されてる"という実機報告を受け調査、
  Round36-9でterrain_gen.pyのMAX_CODEが79→93に増加した結果、自機ショットBG
  (日中用sky/rock、旧codes88-100、group11-12)のパターンコード帯と地形自身の
  パターンコード帯(codes0-93)がcodes88-93で衝突していたことが根本原因と判明
  (両者が同じVRAMパターンジェネレータ領域を取り合い、地形セル側から自機
  ショットの絵柄が透けて見えていた)。自機ショットBGのsky/rock系8コードを
  codes224-239(groups28-29、EQUリテラルを全数精査し実際に未使用と確認した
  領域)へ再配置して解消。`bullet_gen.py`側はコード番号を一切ハードコードして
  いないため、`combined_test.asm`のEQU定数変更のみで完結。VRAM実測で新コードに
  自機ショットの本来のドット形状が、旧コードに地形自身の斜面パターンが正しく
  戻っていることを確認済み。全回帰926 passed/0 failed。**この種の固定コード帯の
  衝突は、地形の遷移数(=terrain_gen.pyのMAX_CODE)がユーザーの編集次第で
  今後も増減しうる以上、再発しうる構造的リスクとして残っている**(パターン
  コード空間全体を動的管理する仕組みへの刷新は今回スコープ外・未実施 -
  詳細はHANDOFF.md Round36-10参照)。
- **(2026-08-30、Round36-11、完了済み)**: "キャラデータ差し替え まずRockのデータを
  添付のものに で、それ以外は自機ショット...3つのデータにわけ1発目水平撃ち
  BulletFU、2発目FM、3発目FLと切り替えてローテーションさせる 斜めも同様に
  UU、UM、ULで切り替え"という指示でRock地形テクスチャの差し替え、および自機
  ショット(水平F・斜めU)の1ポーズ固定→3ポーズ巡回化を実施。実装前にVRAM
  パターンコード予算(BG側・スプライト側両方)を全数調査したところ深刻な
  制約が判明し、ユーザーと直接すり合わせて方針決定: F(水平)はBG側24コード
  予算内でsky/rock/night×左右×3パターンをフルローテーション、U(斜め)は
  ボス戦中のみのBG代替描画は単一ポーズ据え置き、通常時のハードウェア
  スプライト(0-255スロット完全に埋まっており新規確保不可能と判明)は
  発射ごとに`PAT_BULLETU`/`PAT_BULLETU_L`をVRAM上で動的書き換えする方式に
  (3発以上同時表示時は直近発射分と同じ絵になる見た目上の妥協あり、他キャラ
  表示への影響なし)。全回帰923 passed/0 failed(`terrain_render_perf_test.py`
  はgit HEADとの一時的なシンボル名ミスマッチによる既知の失敗、コミット後に
  解消見込み)。詳細・VRAM予算の全数調査結果・実装詳細はHANDOFF.md
  Round36-11参照。
- **(2026-08-30、Round36-12、完了済み)**: "Rockの背景色をダークレッドに...
  ホーミングがたまに自機の上あたりに残る...ホーミングは弾数を増やす...4発
  追加"の3件に対応。(1) Rock背景色: `terrain_gen.py`のROCK_COLOR変更は
  `combined_test.asm`側の既存パッチ`ROCK_COLOR_SWAPPED_PATCH`(過去ラウンドで
  Rockのfgを既に6=ダークレッドへスワップ済み)に上書きされ無効だったと判明、
  実際のfgが6だったためbgもダークレッドにするとfg=bg同色でグリフ消失する
  ところだった(ユーザー自身の懸念が的中)、指示通りブラックにフォール
  バックして`ROCK_COLOR_SWAPPED_PATCH`側を修正。(2) ホーミングのバグ:
  state2→3遷移判定がジャンプ中に一時的に小さくなる`TANK_Y_CUR`を使って
  いたのが原因、ジャンプ非依存の`TANK_GROUND_Y`に変更して解消。(3) 弾数
  4発追加: hwスプライトパターンコード・ATTRIBUTEスロットとも完全に
  空きゼロと確認、`horming_gen.py`に残る「BGは動きがガタガタでスプライト
  必須」という過去の経緯とも矛盾することをユーザーに確認の上、新規4発を
  別枠のBGプール(`HORMING_BG_POOL`)として実装(既存4発のスプライト版は
  無変更)。全回帰940 passed/0 failed。詳細はHANDOFF.md Round36-12参照。
- **(2026-08-30、Round36-13、完了済み)**: Round36-12への3件のフィードバック
  に対応。(1) Rock225の背景色復元: `ROCK_COLOR_SWAPPED_PATCH`の変更を
  完全に取り消し(`061h`→元の`06Bh`)。ROCK_L/ROCK_RとR225の登り/下り
  4種は`terrain_gen.py`のSTEADY_BASEにより物理的に同一VRAMカラー
  グループ(group1)を共有しており、「Rockだけ」を独立して別色にすることは
  1バイトの変更では不可能と判明(過去の色にじみバグ再発リスクのある
  大規模再設計が必要なため、指示なしに着手せず)。(2) ホーミングのBG/
  スプライト同時発射化: 従来の「スプライト4発→BG4発」の順次ブロック方式
  (2プールに分けた意味が半分の期間失われる)を、1ティックで両方から
  1発ずつ同時発射する方式に変更(`HORMING_VOLLEY_COUNT`は「発射ペア数」
  (0-4)に意味変更)。(3) BGホーミングの配色をブルー(bullet側sky配色の
  流用)からブラックへ変更。全回帰941 passed/0 failed。詳細はHANDOFF.md
  Round36-13参照。
- **(2026-08-30、Round36-14 Part A、完了済み)**: "BGホーミングが地形に
  入ったときはSandの背景色になるように ブラックのままだと目立つんで"に
  対応。新規カラーグループは不要と判明 - `terrain_gen.py`のSand専用
  group2(codes16-23)は地形生成器が実際に使うのは2コードのみで、
  18-23の6コードが未使用のまま既にSand本来の色(0xABh)で着色済みだった
  ため、そこへ黒版と同じ5枚のビットマップを複製ロードするだけで対応
  (`HORMING_BG_SAND_SL/DL/DOWN/DR/SR_CODE`=18-22)。`DRAW_HORMING_BG_
  CELL`をY座標由来の行と`BULLET_ROCK_ROW_MIN`(16)で比較し、`ERASE_
  HORMING_BG_CELL`と同じ閾値で黒/Sandの2テーブルを切り替えるよう変更。
  全回帰945 passed/0 failed。詳細はHANDOFF.md Round36-14参照。
  - 同じユーザー発言のPart B(ボススポーン後は自機/ショット/地形以外
    全リソース解放されているという恒常的な留意事項)は継続的な設計方針
    として引き続き念頭に置く。Part Cはこの直後(Round36-14 Part C)で
    完了。
- **(2026-08-30、Round36-14 Part C、完了済み)**: ボスの形態変化を実装。
  実装着手直後にユーザー自身が閾値を訂正: "ボス耐久値が200、、、じゃ
  ないや 50になったらだった 50になったらスパーク爆発しSasapiBrokenに
  変化して インフィニティ起動で回って ランダムタイミングで停止し 少し
  てまた回る これがシーケンスで で、0で最後の爆発で"。既存の死亡スパーク
  演出のSPARKフェーズのみ(GROW/SHRINK/FLASHは使わない)を`BOSS_EXPL_
  REASON`で分岐して再利用、HP<=50(境界含む)の一撃で`BOSS_FORM`という
  新規状態(0=通常/1=SPARK遷移中/2=形態変化後)に一度だけ遷移。Horming/
  Thunder/SBeamの新規発射は全て`UBA_ACTIVE`ツリー内でしかアームされない
  ため、`UPDATE_BOSS_ALL`が二度と`UBA_ACTIVE`を呼ばなくなるだけで
  自動的に新規発射停止(個別ガード不要、既発射分は自然消滅まで継続)。
  移動は「移動→ランダム時間停止→少し停止→また移動」を**永久に繰り返す
  サイクル**として実装(`BOSS_BROKEN_PHASE_END_TICK`を毎回再抽選)、
  8の字(Gerono lemniscate)軌道は`sasapi_gen.py`で事前計算した64点LUT
  を明示インデックス(`BOSS_BROKEN_PATH_INDEX`、移動中のみ進行)で参照 -
  停止→再開時に軌道上の位置が連続するようGAME_TICK直接参照ではなく
  カウンタ方式を採用。「0で最後の爆発で」への対応でボディサイズ変更
  (64x64→32x32)に伴う2件の実害バグを発見・修正: 死亡演出の中心セル
  計算が旧ボディ前提の+32オフセットのままだった点、および死亡時の
  点滅処理が旧ボディのステージングバッファを無条件でフラッシュし
  引退済みの旧ボディが一瞬復活してしまう点。新規テスト`boss_broken_
  form_test.py`41件を追加。実装完了後の全回帰で`sbeam_test.py`が
  無関係に10件失敗する現象を発見・調査した結果、コード側ではなく
  `tests/banked_helpers.py`の`call_routine`ヘルパー自身のデフォルト
  センチネル番地(0x8000)が、今回のコード追加でファイルレイアウトが
  シフトした結果`STAGE_SBEAM`自身の実アドレスとたまたま衝突し、通常の
  直線実行を誤って「関数から戻った」と誤検知していたテストハーネス
  側のバグと判明、実アセンブル済みコードが絶対に存在しない0x0000へ
  センチネルを変更し恒久的に解消(全テスト共通ヘルパーのため今後の
  同種再発を防止)。全回帰986 passed/0 failed。詳細はHANDOFF.md
  Round36-14参照。
  - **保留中**: `BOSS_BROKEN_MOVE_MIN/RANGE_TICKS`・`BOSS_BROKEN_STOP_
    MIN/RANGE_TICKS`・`BOSS_BROKEN_PATH_HOLD_FRAMES`・8の字軌道の中心/
    振幅は全て未調整の初期値(実プレイでのペーシング調整は別途)。HP0の
    「最終爆発」自体は動作するが、その後の演出(ステージクリア画面等)
    は引き続き未実装(下記「保留中タスク」参照)。
- **(2026-08-30、Round36-14 実機フィードバック対応、完了済み)**: "まず
  スクロールの地形のSandがほかのパターンに書き換わってる 前のROMでは
  正常だった で、スパーク爆発後ボスの爆発位置に関係なく右から出てきて
  る 爆発位置からでなきゃおかしい"、続けて"多分ホーミング 何でボス関係
  いじってるだけで地形定義が書き換わるんだよ"(推測は的中)の2件に対応。
  (1) Part Aの`HORMING_BG_SAND_*_CODE`が18-22に置いたのが根本原因と判明
  - `terrain_gen.py`の(Sand,Sand)同一idペアは実は7フレーム分のブレンド
  アニメーション(codes17-23)を使っており、「BLEND_BASEが24からなので
  18-23は空き」という当初の静的推論が誤りだった。エミュレータで実際に
  VRAM内容を調査する方式に切り替え、ブート直後・実プレイ後の両方で
  全バイト0のgroup12(codes96-103)を発見・再配置(この際、ROCK_COLOR_
  SWAPPED_PATCHの対象範囲に入るため今度は明示的なカラーバイト書き込み
  が必要と判明・対応)。(2) 8の字経路LUTが絶対座標だったため、変身の
  瞬間の実位置に関係なく毎回同じ画面右寄りの固定点から出現していた
  問題を修正 - LUTを「変身の瞬間にキャプチャ・クランプする起点
  (`BOSS_BROKEN_ORIGIN_X/Y`)からの符号付きオフセット」方式へ全面書き
  換え。`horming_test.py`にterrain_gen.py側から再計算した期待値との
  直接バイト比較による回帰ガード、`boss_broken_form_test.py`に起点
  キャプチャ・クランプの新規テストを追加。全回帰989 passed/0 failed。
  詳細はHANDOFF.md Round36-14(実機フィードバック対応)参照。
  - **教訓(今後のVRAM予算調査に適用)**: 「使われていないように見える
    パターンコード/カラーグループ」は、コードの静的推論だけで空き
    判定しない。今後は必ずエミュレータでの実VRAM内容確認(ブート直後
    +実プレイ後の両方)を経てから空き判定を行うこと。
- **(2026-08-30、Round36-14 実機フィードバック対応その2、完了済み)**:
  "スパーク爆発で最初からボスが消えてる...消えてしまうことがある 何ら
  かの切り替えタイミングの問題だろう で、インフィニティ軌道はその位置
  から始まるが一旦中央に寄せろ センタリングするかたちで 今だと端で
  倒すと画面半分の狭い起動で動いてしまってる で形態変化後に64x64の
  コリジョンのままになってる 32x32になるよう修正"の3件に対応。
  (1) ボス消失: `TRIGGER_BOSS_BROKEN_FORM`がポーズ中トリガー時の処理を
  `INIT_BOSS_EXPLOSION`から不完全にコピーしており、`DRAW_BOSS`+
  `FLUSH_BOSS_SPRITES`によるスプライト復帰処理が欠けていた(ポーズ中は
  `HIDE_BOSS_SPRITES`済みのため、これが無いと非表示のまま)。プレイヤー
  がポーズ中も攻撃を続ける限り高頻度で発生しうる通常ケースだった。
  (2) 軌道センタリング: 前ラウンドの「死亡位置起点の相対オフセット」
  設計自体が画面端クランプによる狭い軌道を生んでいたため撤回、
  「実際の死亡位置に出現→固定の画面中央点へ歩いて寄る(RECENTERING
  サブフェーズ、新設`STEP_TOWARD`汎用漸近ヘルパー)→中央到達後は
  固定中央点を中心とした絶対座標の8の字軌道(ORBIT)」という2段階
  構成に全面設計変更。切り替え地点は軌道自身の交差点(オフセット
  (0,0)になる点)に固定し視覚的ジャンプを防止。(3) コリジョン32x32化:
  `CHECK_HIT_PAIR_BOSS`のAABB半径をコンパイル時定数から`BOSS_FORM`に
  応じた実行時選択(64/32)に変更。`boss_broken_form_test.py`を全面的に
  書き直し(55件)、全回帰998 passed/0 failed。詳細はHANDOFF.md
  Round36-14(実機フィードバック対応その2)参照。
- **(2026-08-30、Round36-14 follow-up#4、完了済み)**: "SasapiBrokenの
  停止はインフィニティ軌道の1周に１回何処かで停止 で、停止中にビーム
  攻撃をする 添付がそのキャラデータ １から４までの左方向斜め下に順の
  角度でビーム発射...１は２，３の左に４は右に８ｐｘオフセットしたX
  位置になる"(添付4スプライト`SBeam1-4_16x16.json`)に対応。
  (1) 従来のGAME_TICKベースのランダム持続時間MOVING/STOPPEDサイクルを
  廃止し、新設`BOSS_BROKEN_STEPS_TO_STOP`(実軌道インデックス前進のみ
  カウントダウン)による「1周1回停止」へ再設計。(2) 4方向ビーム攻撃を
  新規実装: 添付スプライトのセンターライン座標から厳密な整数比を
  直接算出(beam1:-2:1, beam2:-2:5, beam3:2:5, beam4:2:1)してテーブル化
  - ユーザー自身の角度見積もり("22、77、107、129かと思う")とはどの
  角度換算方式でも一致しなかったため、再現性のあるピクセルデータ由来
  の比率を採用しユーザーへ開示。`STAGE_SBEAM`の誤差蓄積式Bresenhamを
  X方向符号のパラメータ化・実行時画面端バウンドチェックの2点で拡張。
  AskUserQuestionで3点確認(発射タイミング=1→4順次/攻撃種別=本物・
  画面端まで伸びる/表示方式=発射ごとに前ビーム消去・常に1本のみ、
  いずれも推奨案採用) - 特に表示方式は、4本同時発射だと事前Bresenham
  シミュレーションで最大83hwスプライトスロット必要と判明した(予算は
  ~18-24)ため、ユーザーに直接選んでもらって解決。パターンコードは
  旧SBeamとの低頻度衝突リスクを避け新規`PAT_SASAPI+16`〜`+19`を使用。
  `boss_broken_form_test.py`に36件追加(55→91件、特に`FIRE_BOSS_
  BROKEN_BEAM`は独立実装のPython版Bresenhamシミュレータとの全点突合
  で検証)。全回帰1037 passed/0 failed。詳細はHANDOFF.md
  Round36-14(follow-up#4)参照。
- **(2026-08-30、Round36-14 follow-up#4 実機フィードバック対応、
  完了済み)**: 上記ビーム攻撃の実機確認結果、"全然えが違うな 色も
  ブラックで渡したシアンではない で、繋げる必要はない 取り敢えず
  16x16で4本扇状に今の感じでいい スクショのような感じだな"という
  指摘(ボス本体上にピンク色の塊状ノイズが表示されたスクリーンショット
  添付)を受け、`FIRE_BOSS_BROKEN_BEAM`を全面書き直し。根本原因は2つ:
  (1) 添付4スプライトは「1本のビーム全体を描いた完成された1枚の16x16
  絵」だったが、`STAGE_SBEAM`と同じ「1タイルを反復してBresenham線を
  引く」方式をそのまま流用したため、完成絵を対角線上に何枚も重ね
  描きすることになり塊状のノイズになっていた。(2) `BOSS_BROKEN_
  BEAM_COLOR EQU 071h`が、BGパターンカラー用の「fg/bgパック」規約を
  hwスプライトのカラー属性バイト(実際は下位4bit=単一カラーインデックス、
  bit6=EC/Early Clockフラグ)に誤って適用していたため、色が黒になる
  上にECフラグが意図せずONになり位置も32pxズレていた可能性がある。
  Bresenham線描画・画面端バウンドチェック・スロット予算キャップを
  完全に撤廃し、ボス本体近くの固定位置に単発16x16 hwスプライトを
  1枚置くだけのシンプルな実装に変更(繋がった線は不要、扇形レイアウト
  自体は「今の感じでいい」で維持)。カラーは`07h`(プレーンなカラー7=
  シアン、ECフラグなし)に修正。`boss_broken_form_test.py`のビーム
  関連テストを単発スプライト方式に合わせて全面書き直し(91→88件)。
  全回帰1034 passed/0 failed。詳細はHANDOFF.md Round36-14
  (follow-up#4 実機フィードバック対応)参照。
  - **保留・未確定**: `BOSS_BROKEN_LAP_STEPS_MIN/_RANGE`・`BOSS_BROKEN_
    BEAM_INTERVAL`は全て未調整の初期値。実プレイでのバランス調整は
    別途。ビームの固定位置(ボス本体の垂直中心・水平中心±1セル)は
    ユーザーの「今の感じでいい」で確認済みだが、実プレイでの見え方の
    微調整は今後の実機フィードバック待ち。
- **(2026-08-30、Round36-14 follow-up#4 実機フィードバック対応その2、
  完了済み・一部ユーザー回答待ち)**: 上記の色修正版でも実機で
  "全然違うぞ ／＼みたいな線で...グラフィックも壊れてる 色はシアンに
  なったがな"という報告を受け、2つの実バグを特定・修正。(1) 16x16
  hwスプライト1枚は4つの連続パターンコード(TL/BL/TR/BR)を消費するが、
  4本のビームに1つ違いのコード(16,17,18,19)を割り当てていたため各
  ビームの読み込みが直前のビームのデータを一部上書きしていた -
  4つ間隔(16,20,24,28)へ修正。(2) より本質的な原因として、
  `REVEAL_BOSS_BROKEN_FORM`(ファイル前方)が`BOSS_BROKEN_BEAM_CODE1-4`
  (定義がファイル後方)を参照していたため、アセンブラが式中の
  `PAT_SASAPI`を0として誤って評価する前方参照バグを発見・特定
  (`mini_z80asm.py`自体の修正はスコープ外、EQU定義を`PAT_SASAPI`直後
  へ移動する回避策で対応)。**ユーザーの「レンダリングで確認したほうが
  いいな ビルド前にレンダリングしたスクショくれ」という指示を受け、
  以後は実機に送る前に必ずエミュレータでの実VRAM→PNGレンダリングで
  確認する運用を導入**(`render_check.py`の既存レンダラ+Pillowで
  PNG変換)。レンダリングにより角度・色は正しいことを確認できたが、
  ビームのY座標(本体の垂直中心)が本体スプライトと視覚的に重なって
  見えることが判明、ユーザーへ確認依頼した結果 "重なっても問題ない
  そのスクショで正しい これがその角度で直進でOK" と確認済み(追加
  修正なし)。`boss_broken_form_test.py`にVRAM直接比較のテストを2件
  追加(90件)。全回帰1036 passed/0 failed。詳細はHANDOFF.md
  Round36-14(follow-up#4 実機フィードバック対応その2)参照。
- **(2026-08-30、Round36-14 follow-up#4 実機フィードバック対応その3、
  完了済み)**: "ビームが飛んで来ないな 発射タイミングは今でいいが
  4つのパターンはそれぞれ4方向に打ち出すんだぞ 今はボスの上に表示
  されてるだけ それで何の攻撃になる 発射して飛ばすんだよ"という
  指摘を受け、単発静止スプライトを実際に飛ぶ移動弾へ全面書き直し。
  前Round"その2"の"繋げる必要はない"を"動かす必要すらない"と過剰
  解釈していたのが原因(攻撃は元々"本物の攻撃、画面端まで伸びる"と
  確認済みだった)。`BOSS_BROKEN_BEAM_TABLE`を5バイトエントリ
  (XOFS,DXMAG,DYMAG,XDIR,CODE)に復元し、DXMAG/DYMAG/XDIRを「1フレーム
  あたりのpx速度」として再利用。新設4スロット構造体配列RAM
  (`BOSS_BROKEN_PROJ_ACTIVE/X/Y/DX/DY/CODE`)と新設`UPDATE_BOSS_BROKEN_
  BEAM_FLIGHT`(`UPDATE_BOSS_BROKEN_ACTIVE`から毎フレーム無条件に呼び
  出し)で、発射された弾が画面外に出るまで独立して飛び続けるように
  実装。8bit符号なし座標の単純加算だと画面端超えでラップして
  「テレポートしたように見える」危険があるため、加算前に方向別の
  範囲チェックで先に非アクティブ化するガードを実装。発射シーケンス
  自体(1→4の順にINTERVAL間隔)は"発射タイミングは今でいいが"の通り
  無変更。`CHECK_BOSS_BROKEN_BEAM_VS_TANK`は最大4スロットをループする
  方式に変更(命中してもそのスロットは非アクティブ化せず貫通継続)。
  ユーザー指示"レンダリングで確認したほうがいいな ビルド前に
  レンダリングしたスクショくれ"を継続適用、ビームが実際に画面上を
  移動する様子をレンダリングしてユーザーへ提示してから最終確認。
  `boss_broken_form_test.py`を全面書き直し(88→104件)。全回帰1050
  passed/0 failed。詳細はHANDOFF.md Round36-14(follow-up#4
  実機フィードバック対応その3)参照。
  - **保留・未確定**: 各ビームの速度(DXMAG=2固定、DYMAG=1または5)は
    絵の比率をそのままpx/frameへ転用した未調整の値(浅いビーム1,4が
    遅く、急なビーム2,3が速い副作用あり)。画面外判定のマージンも
    未調整。実プレイでのバランス調整は別途。
- **(2026-08-30、Round36-14 follow-up#5、完了済み)**: "ではボス攻撃に
  サウンドを入れる ホーミング、サンダー、サンダービーム、ササピー
  レーザーそれぞれに...こちらで検討するんで作って聞かせてくれ ここで
  Html貼ってくれ そこから選ぶ それぞれ3種作ってみて"という指示で、
  実機PSGエンジン(`SOUND_UPDATE`/`SOUND_CALC_NOISE_GATE_VOLUME`/
  `SU_BOOM`)と同じ仕組みをWeb Audioで再現した試聴ページをArtifactで
  公開、4カテゴリ×3種=12候補を試聴してもらい選定(ホーミング=H2、
  サンダー=T3(フィードバックを受けピッチ引き上げ改訂)、サンダー
  ビーム=S1、ササピーレーザー=L3、最終的に「それぞれ音量は最大で」)。
  選定結果を`combined_test.asm`へ実装 - 新規`SOUND_HORMING`/
  `SOUND_THUNDER`/`SOUND_SBEAM`/`SOUND_SASAPI_LASER`の4ルーチンを
  全て既存の共有チャンネルA/エンベロープ機構(`SND_TIMER`/`SND_DECAY`/
  `SND_NOISE`/`SND_BOOM_DECAY_CTR`)の上に構築、新規エンジンコード
  ゼロ(サンダーは`SOUND_BOSS_BOOM`のboomモードをそのまま流用、
  サンダービームは標準の1:1デューティゲートをトーンchに初めて適用)。
  **実装中に実バグを発見**: `CTTR_FIRE`ラベルは`CHECK_THUNDER_
  TRIGGER_RIGHT`専用の内部ラベルで、`CHECK_THUNDER_TRIGGER_LEFT`は
  全く別の独立した発射末尾を持っていたため、最初はサンダーの左方向
  発射だけ無音のままになっていた - 新規テストでLEFT/RIGHT両方向を
  個別に検証したことで発見・修正。新規`boss_attack_sfx_test.py`
  (25件、各SOUND_*の単体検証+実発射経路からの統合検証)。全回帰1075
  passed/0 failed。詳細はHANDOFF.md Round36-14(follow-up#5)参照。
  - **保留**: 実機での音量・音質バランスの最終確認はユーザーの実プレイ
    フィードバック待ち。
- **(2026-08-30、Round36-14 follow-up#5 実機フィードバック対応、
  完了済み)**: "ボス戦のみボスのダメージ音(キンキン音)カットで
  ボス攻撃音と被ってしまうんで"という指摘を受け、`CHECK_HIT_PAIR_
  BOSS`の`CHPBOSS_NORMAL_HIT`(自機弾がボスに命中した際の既存
  `SOUND_ZUM_DEFLECT`呼び出し)のみ削除。このコールパスは元々
  `BOSS_ACT=1`ガード付きで「ボス戦中にしか鳴らない」音だったため、
  「ボス戦のみ」は追加の条件分岐を要さず単純にこの1箇所を削除する
  だけで実現(他の`SOUND_ZUM_DEFLECT`呼び出し箇所 - Zumバウンド・
  自機がホーミング/サンダー/サンダービームに被弾した際 - は無変更)。
  `BOSS_FLASH_TIMER`(視覚的フラッシュ)は無変更、音声のみカット。
  `boss_attack_sfx_test.py`に4件追加(29件)。全回帰1079 passed/
  0 failed。詳細はHANDOFF.md Round36-14(follow-up#5
  実機フィードバック対応)参照。
- **(2026-08-30、Round36-14 follow-up#6、完了済み)**: "かなり動作速度が
  遅くなったが 無駄な処理が無いか確認 ボス戦に入っているのに ボス以外
  の処理が回っていないか 逆にボスまでにボスのみの処理が回ってないか"
  という指摘を受けMAINLOOPの3層ガード構造(ザコ敵/ボス専用サブシステム
  の相互スキップ)を監査。第1・第2層は過去ラウンドで既に健全と確認、
  発見した実バグは1件: 今Round追加した`CHECK_BOSS_BROKEN_BEAM_VS_TANK`
  (形態変化後4方向ビームの自機衝突判定)だけが、ボス戦全体を通して
  毎フレーム無条件に呼ばれていた。しかしこの判定の`BOSS_BROKEN_PROJ_
  ACTIVE`フラグは`LAUNCH_BOSS_BROKEN_BEAM`(`BOSS_FORM=ACTIVE`の場合
  のみ到達可能)からしかセットされないため、形態変化前(通常フォーム、
  ボス戦の大部分)は4スロット全て構造的に必ず非アクティブと確定して
  おり、無駄なループだった。Homing/Thunder/SBeamは形態変化前に発射
  された個体が変化後も飛び続けるため`BOSS_FORM`ではガードできない
  (意図的に無条件のまま)のとは対照的な非対称性。MAINLOOP側の呼び出し
  箇所に`BOSS_FORM==BOSS_FORM_ACTIVE`ガードを追加。新規
  `boss_perf_gate_test.py`(2件、実際の`step_frame`経由でゲート自体を
  直接検証)。全回帰1081 passed/0 failed。詳細はHANDOFF.md
  Round36-14(follow-up#6)参照。
  - **規模感の補足**: 見つかったのは4スロット分のフラグチェック+
    自機座標計算のみ(毎フレーム)で、体感できるほど大きな影響とは
    考えにくい規模。「かなり」遅くなったという体感の全てをこれで
    説明できるかは未確定 - 修正後も遅く感じる場合は追加調査が必要。
- **(2026-08-30、Round36-14 follow-up#7、完了済み)**: "ロジックとして
  高速化出来そうとか アルゴリズムを変更して高速化は出来ないか"という
  follow-up#6の直接の続き依頼で、毎フレーム無条件実行される
  `UPDATE_BOSS_BROKEN_BEAM_FLIGHT`/`CHECK_BOSS_BROKEN_BEAM_VS_TANK`
  自体の内部ロジックを対象に register-allocation を引き締め。両方とも
  従来「安全優先で毎回メモリからスロットindexを再読込」していたのを
  「ループ内でD/Eが他用途に一切使われないこと」を根拠に1回計算して
  保持し続ける方式に変更(後者は自機座標をB/Cへ退避しPUSH/POP DEの
  スタック往復も全廃)。IX相対アドレッシング案はこのアセンブラが
  `ADD IX,DE`非対応のため断念。T-state実測(4アクティブ時):
  `UPDATE_BOSS_BROKEN_BEAM_FLIGHT` 2963T→2227T(-24.8%)、
  `CHECK_BOSS_BROKEN_BEAM_VS_TANK` 962T→745T(-22.6%)。関連する
  既存テスト(boss_broken_form_test.py 104/boss_perf_gate_test.py 2/
  boss_attack_sfx_test.py 29/boss_collision_test.py 18)全て無変更で
  通過、挙動は完全不変であることを確認済み。全回帰1081 passed/
  0 failed。詳細はHANDOFF.md Round36-14(follow-up#7)参照。
  - **保留・スコープの補足**: 今回は「形態変化後に無条件で毎フレーム
    走る」この2ルーチンのみに絞った調査・実装(最も影響大・最もリスク
    低いと判断)。同種の冗長な再読込パターンが他の新規ルーチン
    (`LAUNCH_BOSS_BROKEN_BEAM`等、毎フレームではなく発射時のみ実行)
    にも残っている可能性はあるが、網羅的な監査は未実施。「かなり」
    遅くなったという体感全体をfollow-up#6+#7でどこまで説明できて
    いるかは依然未確定 - 体感が改善しない場合は追加調査が必要。
- **(2026-08-30、Round36-14 follow-up#8、完了済み)**: "バグはないよう
  だ ではループ展開を検討 現在どれほどの容量に達してるかによるが"の
  指示で、follow-up#7と同じ2ルーチンを4スロット完全展開(loop
  unrolling)。着手前に実容量を計測(このビルドの唯一のハード上限は
  bank0+bank1合計32768バイト、`ORG`はファイル先頭1箇所のみで
  bank0固有の制限は実在しない - 着手時点で残り4315バイト)。DE実行時
  インデックスをコンパイル時定数アドレス(`LD A,(BASE+N)`)に置き換え、
  1フィールド読みは28T→13T、読み書きペアも同バイト数で53T→44T
  (`JP cc,nn`が成立/不成立問わず常に10T固定な点を活用)。T-state実測
  (follow-up#7からの通算): `UPDATE_BOSS_BROKEN_BEAM_FLIGHT`
  2963T→1292T(約56%減)、`CHECK_BOSS_BROKEN_BEAM_VS_TANK`
  962T→326T(約66%減)。ROM総コストは+256バイト(28453→28709、
  残り容量4315→4059バイト)。**開発中に実バグを発見・修正**:
  このアセンブラの`eval_expr`は演算子優先順位を持たず左から右へ
  逐次評価するため、`BASE+N*4`が`(BASE+N)*4`という誤ったアドレスに
  評価される(`boss_broken_form_test.py`が4件失敗して発覚)- Python
  コード生成側で`N*4`を先に計算し単一定数として埋め込む形に修正。
  関連既存テスト153件(boss_broken_form_test.py 104/boss_perf_gate_
  test.py 2/boss_attack_sfx_test.py 29/boss_collision_test.py 18)
  全て無変更で通過、全回帰1081 passed/0 failed。詳細・ROM総コストが
  単純合算(+406)より少ない(+256)理由(ファイル末尾の`ALIGN 256`
  パディング量の偶然の相殺、バグではないことを検算済み)はHANDOFF.md
  Round36-14(follow-up#8)参照。
  - **保留**: 残り容量4059バイトは今後の全新機能と共有される唯一の
    予算。同種の展開を他のホットパス(`LAUNCH_BOSS_BROKEN_BEAM`等)に
    広げる余地はあるが、都度費用対効果を判断すべきで網羅的監査は
    未実施。
- **(2026-08-30、Round36-14 follow-up#9、完了済み)**: "ボス以外での
  効果的な改善はないか"の依頼でExploreエージェントによる非ボス系ホット
  パス調査を実施、続けて"Update enemyに絞って実測してくれ"の指示で
  `UPDATE_ENEMIES`(`UE_UPDATE_ALL`)/`CHECK_BULLET_VS_ENEMY`
  (`CHECK_HIT_ONE_BULLET`)を対象に実装・実測。非ボス系にはボスと同種の
  `ADD HL,DE`再計算は無い(元々`(IX+d)`直接アクセス)代わりに、
  「スロット送りを`INC IX`×9(ENEMY_SLOT_SIZE分)+DJNZ+PUSH/POP BC」で
  行うオーバーヘッドが判明。ENEMY_SLOT_COUNT=3固定を活かし、DJNZループ
  を「`LD IX,ENEMY_POOL(+9)(+9)`→`CALL`」3回の明示展開に変更(呼び出し
  先の`UPDATE_ONE_ENEMY`/`CHECK_HIT_PAIR`自体は複製せず共有のまま -
  ボスの2ルーチンと違い「ループ展開特有の4倍コード」が発生しない)。
  T-state実測: `UPDATE_ENEMIES`(3スロット稼働)3446T→3100T(-10.0%)、
  `CHECK_BULLET_VS_ENEMY`(3x3全ミス)3259T→2221T(-31.9%)。**ROMコストは
  むしろ-20バイト**(ラッパー自体が35→25/32→22バイトへ縮小、ボスの
  時と違い速度とサイズのトレードオフが発生しない純粋な改善)。3スロット
  全てが独立して正しく動作・命中判定されることをその場でテスト作成し
  検証、全回帰1081 passed/0 failed。詳細はHANDOFF.md Round36-14
  (follow-up#9)参照。
  - **保留**: 同じ「固定小スロット数をDJNZ+INC IX/IYで歩く」パターンは
    `UPDATE_ZUM_ALL`/`CHECK_BULLET_VS_ZUM`(ZUM_SLOT_COUNT=2)、
    `UPDATE_FLYER_ALL`/`CHECK_BULLET_VS_FLYER`(FLYER_SLOT_SIZE=11)にも
    存在する可能性が高い(Exploreエージェント調査による指摘、未着手)。
    ROMコストゼロで速度改善という有利な性質のため優先度は上げてよいが、
    今回の指示はUPDATE_ENEMYに限定されていたため未着手。
- **(2026-08-30、Round36-14 follow-up#10、完了済み)**: "ではそれらも
  検討し実測"の指示で、follow-up#9の申し送り通り`UPDATE_ZUM_ALL`/
  `CHECK_BULLET_VS_ZUM`/`UPDATE_FLYER_ALL`/`CHECK_BULLET_VS_FLYER`
  (いずれもスロット数2)に同じ展開(DJNZ+INC IX/IY+PUSH/POP BC→
  固定スロット数ぶんの明示的CALL、呼び出し先本体は複製せず共有)を
  適用。T-state実測: `UPDATE_ZUM_ALL` 3031T→2815T(-7.1%)、
  `CHECK_BULLET_VS_ZUM` 2173T→1525T(-29.8%)、`UPDATE_FLYER_ALL`
  4329T→4053T(-6.4%)、`CHECK_BULLET_VS_FLYER` 2353T→1525T
  (-35.2%、4ルーチン中最大改善率)。ROMコストは4ルーチン合計で
  むしろ-72バイト(follow-up#9と同じくラッパー自体が縮小)。開発中に
  Zumの命中判定テストで2件失敗したが、原因はコード側バグではなく
  「Zumは自機がZumより背後にいる場合のみ実際に破壊される(前面は
  弾を無効化するだけ)」という既存仕様をテスト側が考慮していなかった
  だけと判明、テスト条件を修正して解消。全回帰1081 passed/0 failed。
  follow-up#9と合わせ、Exploreエージェントが指摘した非ボス系の
  主要候補(ENEMY/ZUM/FLYERの計6ルーチン)への対応が完了(BigZum/
  Etankはスロット数1でこのパターン自体が存在せず対象外)。詳細は
  HANDOFF.md Round36-14(follow-up#10)参照。
  - **(2026-08-30、ユーザー確認)** "バグなし良好 これでかなり速度が
    改善された 許容範囲内" - follow-up#6(ガード漏れ監査)〜#10
    (Zum/Flyer展開)にわたる一連の速度改善作業について、バグ無し・
    体感速度の大幅改善・ROM残り容量(4059バイト、follow-up#8時点)も
    許容範囲内との最終確認を得た。**この一連の速度改善作業はここで
    一区切り**(「かなり動作速度が遅くなった」という当初の実機報告
    への対応が完了と判断)。追加の速度改善(他ルーチンの監査等)は
    指示なしに着手しない。
- **(2026-08-31、Round36-14 follow-up#11、完了済み)**: "ではザコ敵の
  弾発射実装 ZakoII2種は反転時に添付データEBullet発射 コリジョンは
  左上4x4ドット 発射タイミングの瞬間の自機を狙って直進 発射は16方向
  Flyerは画面左端まで行き反転後発射 Etankはスポーン後左へ32px移動
  したら発射し方向は左直進のみ 弾はBG使用ファイルEtankBullet"の指示で
  ZacoII/Flyer/Etankの弾発射を実装。着手前にVRAM予算をAskUserQuestion
  で2往復確認: (1) EBullet(ZacoII/Flyer弾)の描画方式 - 過去ラウンドの
  「HWスプライト完全に空きゼロ」を踏まえBGを提案したところユーザーから
  再調査指示、実測(fresh_cpu()+約6000フレームの実プレイで全VRAM内容
  確認)の結果パターンコード80個・ATTRIBUTEスロット6個が実際に未使用と
  判明(旧情報は古かった)、HWスプライトで実装決定(コード234-237、
  スロット26-29)。(2) EtankBullet(BG)側は別の制約を発見 - BGのカラー
  テーブルは8コード単位(1グループ)ごとに1色のみで32グループ全て
  既に色割当済み、ユーザー指定色(fg8/bg11)に完全一致する空きグループ
  はなし、"背景色一致は必要なので文字色が近いものを使ってくれ"との
  回答でgroup31(SkySand専用)の既存色(fg5/bg11)をそのまま流用する形で
  決定(新規カラー書き込みなし)。16方向照準は乗算・除算不要の軽量な
  オクタント折り畳み+密サンプリングLUT自動生成方式で実装
  (`ebullet_gen.py`)、開発中にしきい値の粗い近似(2*minor>major、
  約26.57度)が水平・垂直の弾を22.5度傾けて発射する実害バグとして
  発覚・修正(5*minor>major、tan(11.25度)の中点近似に変更)。EtankBullet
  はHORMING_BG_POOLの`ERASE_HORMING_BG_CELL`/`HORMING_BG_CELL_ADDR`を
  フィールドレイアウトを合わせることでコード複製せず直接再利用。
  開発中にEBULLET_POOLとRAMステージングバッファのINIT時プライミング
  漏れ(既存のENEMY_SPRITE_ATTRS等と同じ仕組みが未実装)によるスプライト
  ゴミ表示バグも新規テストで発見・修正。ビルド前にVRAMレンダリング
  スクショをユーザーへ送付・視覚確認(ユーザーの既存の運用指示に
  従う)。新規`ebullet_test.py`(27件)/`etank_bullet_test.py`(18件、
  いずれも実MAINLOOP通しプレイでの発射確認込み)、全回帰1126
  passed/0 failed。ROM容量29565/32768(残り3203バイト)。詳細は
  HANDOFF.md Round36-14(follow-up#11)参照。
  - **保留**: ボス戦開始瞬間に飛行中の弾は消えずに固まる(低頻度の
    エッジケース、明示的な一括非表示は未実装)。速度・16方向しきい値の
    斜め方向1ステップ丸めは未調整の初期値。残り容量3203バイトは今後の
    全新機能と共有される唯一の予算。
- **(2026-08-31、Round36-14 follow-up#11 実機フィードバック対応、
  完了済み)**: "まずEBulletが全く違うパターン Etankの方は合ってるが
  発射位置がおかしい 16px上に描画されてる...Etankはそもそも32x16しか
  使っていない...無駄に32x32で定義してないか"という実機報告で2件の
  バグを特定・修正。(1) EtankBullet位置ズレ: `LAUNCH_ETANK_BULLET`が
  Etankの生Y座標`(IX+2)`をそのまま使っていたが、実際の見た目
  (`UOET_DRAW`のBL/BR)は`(IX+2)+16`にしか描画されない(ユーザーの
  指摘通り32x16しか使っていない)。`ADD A,16`を追加して解消。
  (2) EBulletパターン衝突: follow-up#11着手時のVRAM予算調査
  (非ゼロ内容チェック)が、「現在ゼロ」と「本当に未割当」を混同
  していたと判明 - `PAT_FLYER`(220-235)/`PAT_FLYER_L`(236-251)は
  16コード分の複数ポーズ用ブロックのうち一部だけが実絵柄を持ち、
  残りは正当な意図的ブランク(このファイル自身の`HUD_ROW_BLANK_CODE`
  と同じ「絵柄は空でも予約済み」パターン)で、単純な非ゼロチェックでは
  区別できなかった。全PAT_*/*_CODE EQUを横断的に洗い出した結果、
  コード0-255は隙間なく割当済みと判明、`PAT_EBULLET`を`SBEAM_CODE`
  (252-255、ボス専用・`S2_BOSS_SPAWN`時にのみロード)の再利用に変更
  して解消(`HORMING_SPR_BASE_SLOT`のATTRIBUTEスロット再利用と同じ
  「ボス専用リソースの安全な使い回し」)。`ebullet_test.py`に直接
  バイト比較+再利用安全性の新規テスト3件追加、`etank_bullet_test.py`
  のアサーション更新、全回帰1129 passed/0 failed。修正後に再度VRAM
  レンダリングで視覚確認しユーザーへ送付。詳細はHANDOFF.md
  Round36-14(follow-up#11実機フィードバック対応)参照。
  - **教訓**: 「実VRAM内容の非ゼロチェックで空き判定する」だけでは
    不十分(意図的なブランク埋め草と真の空きを区別できない) - 今後は
    必ず全LDIRVM呼び出し元をシンボルテーブルベースで横断的に洗い出し、
    実バイト数ベースで空きコードを特定すること。
- **(2026-08-31、Round36-14 follow-up#11 実機フィードバック対応その2、
  完了済み)**: "まずEtankBulletを差し替え 1pxズレているがBGなので
  画像で修正 次にFlyerの弾の発射が反転後の左上になってる 定義は
  32x32になってるんで 起点を0にするとダメ 必ず右向きになる 位置的には
  Flyerの右にYオフセット19pxの位置"の2件に対応。(1) EtankBullet:
  添付の修正版JSON(1px下にシフト)へ`sprites/EtankBullet_8x8.json`を
  差し替えるのみ(BG方式のためコード側は無変更)。(2) Flyer発射位置:
  `UPDATE_ONE_FLYER`のPHASE0→1遷移がまさに「X=0へクランプした直後」
  の1行で発生するため、`(IX+1)`をそのまま発射起点に使うと常に画面
  左上になるバグだった。Flyerの実アートは32x32キャンバスのため、
  正しい起点はFlyer自身の右端(X+32)・Y+19オフセットに修正。
  `ebullet_test.py`にFlyerのX=0クランプシナリオを直接再現する新規
  テスト3件、`etank_bullet_test.py`に新画像との直接バイト比較1件を
  追加。全回帰1133 passed/0 failed。修正後、実際にFlyerがPHASE遷移
  する実フレームを特定してレンダリングし、両方とも正しく表示される
  ことを視覚確認しユーザーへ送付。詳細はHANDOFF.md Round36-14
  (follow-up#11実機フィードバック対応その2)参照。
- **(2026-08-31、Round36-14 follow-up#12、完了済み)**: "Flyerの動作変更
  まずスポーンから32px左に移動したら 添付データのMineを放物線で投下
  着地や自機への被弾で16x16ｐｘの爆発エフェクトとサウンド...次に
  現在はFlyer帰還でSandskyに被ってしまってるので帰還時の右移動のY位置を
  8px上に 右斜め下移動の最終Y座標って事ね その後FlyerLaser発射...
  自機は狙わず右方向水平撃ちのみ BG使用"の3件に対応。(1) Flyer自身の
  PHASE1(帰還)→PHASE2(退場)遷移時のYを、SandSkyに重なる降下方向の
  出口2箇所限定で-8px(新設`UOFL_HOME_DESCEND_EXIT`)。(2) Mine投下:
  `FLYER_SPAWNX`が全インスタンス共通の固定定数と判明したため専用の
  スポーンX記録は不要、32pxしきい値はコンパイル時定数比較のみで判定。
  スプライト空間の全数監査(シンボルテーブル横断+実カラーテーブル
  実測)により**コード0-255が完全に空きゼロ**(ボス専用の再利用可能
  領域も全て二重・三重に専有済み、かつBigZum/Flyerは同時生存しうる
  ため三重目の再利用は新規視覚破損リスク)と判明、ユーザーの「取り
  敢えずスプライトだが」という暫定フレーミングにもかかわらず**Mineは
  最初からBGレンダリングで実装**(EtankBulletと同じ判断)。着地・
  自機被弾いずれも既存の`PAT_EXPLOSION`/`SOUND_DESTROY`(他エネミーの
  死亡演出と同一資産)を再利用、爆発中のみ新規のhwスプライトATTRIBUTE
  スロット(30-31、EBulletの残り)を借用。(3) FlyerLaser: EtankBulletと
  同じerase-then-move-then-drawのBGセル手法をそのまま再利用、発射
  起点はFlyer自身の右端+Y19オフセット(EBulletの発射起点修正と同一
  規約)。色はgroup31(SkySand本来のfg5/bg11、EtankBulletと同じ再利用
  グループ)を選択(降下帰還の終端=SandSky帯域が発射位置のため)。
  新規`mine_flyerlaser_test.py`42件、`terrain_render_perf_test.py`
  (独自のテーブル結合重複によるインポート漏れクラッシュ)と
  `vdp_wait_test.py`(生OUT命令サイト数ドリフトガード)の2件を修正。
  全回帰1175 passed/0 failed。ビルド前にVRAM→PNGレンダリングで
  Mine落下・着地爆発・FlyerLaserの3シーンを視覚確認済み。ROM容量
  30357/32768バイト(残り2411バイト)。詳細はHANDOFF.md Round36-14
  (follow-up#12)参照。
  - **保留**: `MINE_GRAVITY`/`MINE_VX`/`MINE_ANIM_INTERVAL`/
    `FLYER_LASER_SPEED`は全て未調整の初期値。FlyerLaserの色(fg5、
    指定fg7からの近似)・Mineのレンダリング方式(BGで実装したこと)は
    実機フィードバック待ち。
- **(2026-08-31、Round36-14 follow-up#12 実機フィードバック対応、
  完了済み)**: "まずMine投下速度が早すぎる で、投下も真下になってるが
  Flyerの左な 放物線も出てない 次にFlyerLaserのBG背景色がイエローに
  なってる 背景と同じくライトブルーだぞ レーザー自体はシアン"の2件に
  対応。(1) Mine: 毎フレーム無条件の重力加算が速すぎて左ドリフトが
  視認できる前に着地していたのが原因、`MINE_SLOT_SIZE`を7→8に拡張し
  重力を「MINE_GRAVITY_INTERVAL(4)フレームに1回だけ+1」方式へ変更、
  `MINE_VX`も1→2に倍増(Pythonで事前シミュレーションし15-30フレーム・
  水平30-60pxの明確な斜め軌道になることを確認)。(2) FlyerLaser:
  前段のgroup31(SkySand想定)選択が実機で誤りと判明、実際の背景は
  素直な空(bg5)だった。group17(NIGHT_CODE/Mine自身と同じグループ、
  fg1/bg5)へ移設、bg完全一致・fgはシアン(7)の代わりに黒(1)という
  近似(fg7+bg5の組み合わせを持つグループが全32グループ中に1つも
  存在しないと確認済み、group17/group27の塗り替えは既存の夜間パレット/
  Thunder演出への影響懸念から不採用)。`mine_flyerlaser_test.py`の
  該当テストを新仕様に合わせて修正、全回帰1176 passed/0 failed。
  VRAM→PNGレンダリングで両方の修正を視覚確認しユーザーへ送付。詳細は
  HANDOFF.md Round36-14(follow-up#12実機フィードバック対応)参照。
  - **保留**: レーザーの黒色化(シアンからの近似)への追加フィードバック
    次第でgroup17自体の塗り替えも再検討の余地あり。落下物理定数は
    全て未調整の初期値。
- **(2026-08-31、Round36-14 follow-up#12 実機フィードバック対応その2、
  完了済み)**: "まずFlyer 色は置いておいてMine投下直後か直前 一瞬違う
  位置にFlyerが表示されてる で投下位置も本体の左に来てない 次に反転時
  のBullet発射は削除 Flyerレーザーは別の色無いのか 流石にブラックは
  レーザーに見えない"の3件に対応。(1) **実バグ**: `ALLOC_MINE_SLOT`が
  `LD IX,MINE_POOL`でIXを再利用するのに、呼び出し元`UOFL_CRUISE_STEP`
  がCALL直後の`JP UOFL_DRAW`でFlyer自身のIXをまだ前提にしていたため、
  Mine投下フレームだけFlyerがMINE_POOLのデータを誤って自分の座標として
  描画していた(`PUSH IX`/`POP IX`で解消)。(2) Mine投下位置を本体中心
  (+16)から本体左端(生X)に変更。(3) `UOFL_CRUISE_MOVE`(反転地点)に
  残っていたEBullet発射コードを完全削除(Mine/FlyerLaserという新しい
  攻撃手段があるため不要と判断)。(4) FlyerLaserの色をgroup17(fg1黒/
  bg5、背景完全一致)からgroup27(fg7シアン/bg1、ビーム完全一致)へ
  再移設 - 全32グループにfg7+bg5の組み合わせは存在しないため、
  「レーザー自体はシアン」という要望を優先し背景(黒箱)を妥協。
  `ebullet_test.py`/`mine_flyerlaser_test.py`を更新、全回帰1177
  passed/0 failed。VRAM→PNGレンダリングで全て視覚確認済み。詳細は
  HANDOFF.md Round36-14(follow-up#12実機フィードバック対応その2)
  参照。
  - **保留**: FlyerLaserの黒背景への追加フィードバック次第で
    group17/group27自体の塗り替えも再検討の余地あり。
- **(2026-08-31、Round36-14 follow-up#12 その3、完了済み)**: "じゃあ
  ホワイトで ライトブルーにホワイトは使えるんだな てかスプライトの
  空きの話は? 6個くらい空いててて敵弾追加しただけだぞ3"に対応。
  bg5(ライトブルー)を持つグループは全32グループ中3つのみで、空き
  コードがあるのはgroup17だけ(色はfg1固定)と再確認した上で、group0の
  実在するfg15/bg5の組み合わせをgroup17へ複製する形で対応 -
  `NIGHT_COLOR`をfg1/bg5(015h)からfg15/bg5(0F5h)へ塗り替え、
  FlyerLaserのパターンコードもgroup17(139)へ戻した。副作用として
  NIGHT_CODE(夜間演出の先頭行)とMine自身も白系統に変化するが、実際に
  レンダリングして確認したところ夜間演出への影響は先頭1行の一時的な
  色変化のみで実害なしと判断。スプライトATTRIBUTEスロットの内訳
  (EBullet4個+Mine爆発2個=6個でちょうど使い切り)もユーザーへ回答。
  `cloud_changes_test.py`のNIGHT_COLOR直接比較テストを更新、全回帰
  1178 passed/0 failed。詳細はHANDOFF.md Round36-14(follow-up#12
  その3)参照。
- **(2026-08-31、Round36-14 follow-up#12 その4、完了済み)**: "了解
  これでいい で、FlyerのMine投下の放物線をもう少しX方向に広げて前に
  投下するように 合わせてタイミング変更 自機位置を見て自機の64px
  手前に来たら投下 Etankの耐久値−1"の2件に対応。(1) Mine投下トリガー
  を固定距離(スポーンから32px)から自機基準(`TANK_X+MINE_DROP_LEAD_X`
  (64)px手前で発射、毎フレームTANK_Xを実読み)へ変更、`TANK_X`の
  実測上限(~226)を踏まえ閾値の8bitオーバーフローをキャリーフラグで
  検出し即時発射にフォールバックする安全策を実装。`MINE_VX`を2→3に
  増加し、64pxのリード距離と着地までの水平ドリフト量がほぼ一致する
  ようPythonで再シミュレーション。(2) `ETANK_HP_INIT`を8→7に変更。
  `mine_flyerlaser_test.py`の実Flyer統合テストを新トリガーに合わせて
  全面書き直し(オーバーフロー安全性テスト含む)、`etank_unit.py`の
  HP初期値テストを更新。全回帰1180 passed/0 failed。VRAM→PNGレンダ
  リングで自機64px手前での投下・拡大した放物線を確認しユーザーへ
  送付。詳細はHANDOFF.md Round36-14(follow-up#12その4)参照。
- **(2026-08-31、Round36-14 follow-up#12 その5、完了済み)**: "Mine
  投下速度を少しだけ下げて で ZakoII2種の弾も速度を下げて"に対応。
  `MINE_GRAVITY_INTERVAL`を4→5(重力の加算頻度を下げ約15%緩やかに)、
  `ebullet_gen.py`の`EBULLET_SPEED`を3→2に変更(16方向DX/DYテーブルは
  自動生成のためコード変更不要)。両定数ともテストがシンボルテーブル
  経由で動的参照する実装だったため既存テストは無変更で通過。全回帰
  1180 passed/0 failed。詳細はHANDOFF.md Round36-14(follow-up#12
  その5)参照。
- **(2026-08-31、Round36-14 follow-up#13、完了済み)**: "ボスの形態変化
  が今はHP50だが100に変更 で、自機ショットが今は間欠発射だと思うが
  ウェイトはいくつになってる"に対応。`BOSS_BROKEN_HP_THRESHOLD`を
  50→100に変更(ボスHP初期値255の範囲内)。自機ショットのウェイトは
  `SHOT_COOLDOWN_FRAMES EQU 8`(既存値、質問への回答のみでコード変更
  なし)。全回帰1180 passed/0 failed。詳細はHANDOFF.md Round36-14
  (follow-up#13)参照。
- **(2026-08-31、Round36-14 follow-up#13、完了済み)**: 自機ショットの
  ウェイト値(SHOT_COOLDOWN_FRAMES)を尋ねられた回答を受けて"4フレーム
  にして 3発制限を4発に変更"に対応。自機ショットのHWスプライトATTRIBUTE
  スロット枯渇問題(32個全て専有済み)をユーザーに提示し、"Mineは
  演出なのでMineを削ってくれ 2発同時はまず起こらないんで"との確認を
  得た上で、Mine爆発の2枠(30-31)を1枠(30のみ共有)に削減して解放
  したスロット31を斜め弾4発目に割り当て。`SHOT_COOLDOWN_FRAMES`を
  8→4、`BULLET0/1/2_ACT`(F1xxブロック、空きゼロ)を直接拡張できない
  ため新設`BULLET3_ACT`はC1xx領域に配置、`TRY_SPAWN_BULLET`/`SET・
  GET_BULLET_VARIANT`/`UPDATE_BULLETS`/敵側の当たり判定8箇所全てを
  4スロット対応に拡張。**副次的発見**: `tools/z80emu.py`自身の
  `SBC HL,rr`実装がZ/Sフラグを一切更新しないバグを発見・修正(実機の
  本物のZ80やこのROMが動く実エミュレータには影響しない、あくまで
  このプロジェクトのPythonテストハーネス自体のバグ - `SET/GET_
  BULLET_VARIANT`の per-slot ポーズ記憶が常に最後の分岐にフォール
  バックしていたのを発見)。新規`player_shot_pool_test.py`(26件)、
  全回帰1205 passed/0 failed。VRAM→PNGレンダリングで水平弾4発・
  斜め弾4発(4発目もHWスプライト正常表示)を確認しユーザーへ送付。
  詳細はHANDOFF.md Round36-14(follow-up#13)参照。
- **(2026-08-31、Round36-14 follow-up#14、完了済み)**: "エミュレータは
  問題ないのだが実機確認すると地形スクロールが乱れてる Rockのペアの
  16x8の内左側8x8のみおかしい"、続けて"分かった 一つ前のビルドでも
  地形は乱れるが実行してから最新を実行すると正しく動く どこかで初期化
  ミスしてるな で、1回だけだがスタート直後にFlyerレーザーがいきなり
  飛んできた"に対応。根本原因: `ETANK_BULLET_ACT`(follow-up#11)・
  `MINE_POOL`(follow-up#12)・`FLYER_LASER_ACT`(follow-up#12)の3つが、
  実装した一連のラウンドを通じて一度もINIT時に明示的にゼロクリアされ
  ていなかった。`ENEMY_POOL`/`ZUM_POOL`/`BIGZUM_POOL`/`FLYER_POOL`/
  `EBULLET_POOL`/`ETANK_POOL`自身は全て「バッファ全体を明示的にゼロで
  埋める」という既存のINIT規約に律儀に従っていたのに、後から追加した
  この3箇所だけ踏襲し忘れていた。このバグがテストで一度も検出されな
  かった理由は、`tests/banked_helpers.py`の`fresh_cpu()`が使う
  `BankedMem`の`self.flat`がPython標準の`bytearray(0x10000)`(常に
  全ゼロ初期化)だったため - 「RAMは電源投入直後から全ゼロ」という、
  実機では成り立たない前提をテストハーネス自体が暗黙に持っていた。
  ACTがたまたまゴミの非ゼロ値だと、実際は何もスポーンしていないのに
  各UPDATE系ルーチンがゴミのX/Yを座標として扱い、Mine/EtankBulletが
  共有する`ERASE_HORMING_BG_CELL`経由で地形のBGネームテーブルに不正な
  書き込みをしてしまう(スクロールでその誤書き込みが流れて見えるため
  「地形スクロールが乱れている」ように見える)。"1つ前のビルドを実行
  してから最新を実行すると直る"という報告は、電源を切らない再ロードで
  はRAMが前回起動時の状態を引き継ぐという典型的な初期化漏れの症状と
  整合。`ETANK_BULLET_ACT`/`ETANK_BULLET_X/Y`/`ETANK_SPAWN_X`/
  `ETANK_BULLET_FIRED`・`MINE_POOL`全体+`MINE_SPRITE_ATTRS`(Y=209
  プライミング)・`FLYER_LASER_ACT/X/Y`をINIT時に明示的にゼロクリア
  する修正を追加。新規`init_ram_poison_test.py`(48件) - RAM全体を
  0xFF/0xAA/0x55/0x01で汚染してから実際にINITトレースを1から実行し、
  この3つだけでなく既存の全主要プールも横断的に「本当にゼロになるか」
  を検証する回帰ガードを新設。開発中に自分自身のテストの誤り
  (`FLYER_POOL`を「完全ゼロ」でアサートしたが、`ENEMY_POOL`と同じく
  各スロット自身のSPRIDXを意図的に非ゼロ設定する仕様だったため誤検知)
  を発見・修正。全回帰1253 passed/0 failed。詳細はHANDOFF.md
  Round36-14(follow-up#14)参照。
- **(2026-08-31、Round36-14 follow-up#14 続き、構造的な再発防止・
  完了済み)**: "それは何度も何度もやらかしてて 常にこっちで指摘して
  るのに これで通算5回はやらかしてる OSやコンパイラ前提でしか考え
  られないんだな"という直接の批判を受け、直前の`init_ram_poison_
  test.py`(手作業で対象シンボルを列挙したチェックリスト方式)を、
  実際のアセンブラシンボルテーブルから`_ACT`/`_POOL`で終わる全RAM
  シンボルを自動列挙する方式に全面書き換え(手作業のリスト
  メンテナンスを排除し、将来のエンティティ追加でのクリア漏れを
  機械的に検出できるようにした)。この自動発見版を実行した結果、
  即座に2件の未知の候補(`SBEAM_ACT`・`THUNDER_POOL`、いずれもボス
  専用)がTier A(INIT直後にゼロ)で不合格になったが、`combined_
  test.asm`を調査した結果これは見落としではなくround23由来の正当な
  設計(`SKIP_BOSS_SUBSYSTEMS`ゲートにより`BOSS_ACT==0`の間はこれらを
  読む処理が物理的に一度もCALLされず、`BOSS_ACT`が0→1になる実
  スポーン遷移が両方をアトミックにゼロクリアしてから初めて到達可能に
  なる、ユーザー自身のround23指示"ボス前とボススポーン後は完全に
  分けて一切干渉しない...初期化もボス用はボススポーン直前"に基づく
  遅延初期化)と判明。除外リストに戻すと同じ再発リスクを抱えるため、
  Tier Aに落ちたシンボルだけ救済判定Tier B(汚染RAMのまま実際に
  `BOSS_SPAWN_TICK`までMAINLOOPを進めボスを実スポーンさせ、その時点
  で改めてゼロになっているかを実行時に直接検証)を実施する2段階方式
  に設計。`BOSS_ACT`自身はTier Bへのフォールバックを許さず常にTier A
  必須と個別に固定検証。全32件PASS(15秒、pypy3)。全回帰`run_all.py`
  **1237 passed/0 failed**(テスト件数48→32件への変化のみ、ASM変更
  なし・ROM再ビルド不要)。詳細はHANDOFF.md Round36-14(follow-up#14
  続き、構造的な再発防止)参照。
- **(2026-08-31、Round36-14 follow-up#15、実機フィードバック対応・
  完了済み)**: "地形スクロールがカクカクしてるな ボス戦後なにも無く
  ても滑らかに動いてない 周期的に一瞬止まってる スクロール処理で
  不均一な部分があるはず ループがおかしいとか 前はそんな事はなかった
  ぞ"に対応。このROMはHALT/vsync同期を持たない設計で1フレームの
  T-state消費量がそのまま実時間ペースに直結するため、MAINLOOPの
  `TICK AND 07h==0`ゲート(8フレームに1回、地形IDキャッシュ更新を
  行うブロック)が他フレームよりT-stateコストが約5%重いことを実測で
  確認 - ユーザーの診断と一致。内訳を計測した結果、その95%以上が
  `REFRESH_IDCACHE_33`の4回呼び出しそのものと判明。同ルーチン内の
  `LD D,TERRAIN_LUT/256`がループ不変(Dは一度も変化しない)にも
  関わらず33回毎回再実行されていたのをループ外へ引き上げ(出力は
  旧版とビット単位で同一、`terrain_render_perf_test.py`の既存等価性
  検証で確認)。実プレイ相当シナリオでの8フレーム毎スパイク比率は
  1.052→1.033に改善(超過分5.2%→3.3%、約36%削減)。旧コミット
  (`e4e134a`、Round36-14着手前)でも同じ相対スパイク比率を確認して
  おり、この周期的な重さ自体は今回のセッションでの新規退行ではなく
  元々の設計に内在するもの(ボス戦後の静かな待機状態という今回新設
  のシーンを実機で長時間観察して初めて気づいた可能性が高い)。
  完全解消(4回のリフレッシュを複数フレームへ分散)は地形4行間の
  IDCACHEとROWPHASE_Tの整合性が一時的に崩れる視覚的リスクを伴う
  ため、今回は安全な部分最適化のみに留めスコープ外とした。全回帰
  `run_all.py` **1237 passed/0 failed**。ROM容量変化なし(30357
  バイト)。詳細はHANDOFF.md Round36-14(follow-up#15)参照。
  - **(2026-08-31、ユーザー確認)** "その見解で正しいようだ 全体的に
    処理が以前より増えた結果カクつきが目立つようになってるだけだな
    まあ実際は気になるレベルでは無いが エミュレータは内部処置が早い
    ので見えにくいだけだな" - 上記の解析(周期的な重さ自体は今回の
    セッションでの新規退行ではなく元々の設計に内在するもので、
    セッション全体を通じた機能追加の積み重ねにより全体的な処理量が
    増え、相対的に目立つようになっただけ)を実機で確認し、正しいとの
    確認を得た。実害としては許容範囲内("気になるレベルでは無い")との
    最終判断。**この件はここで一区切り** - 追加のスクロール高速化
    (4回のリフレッシュを複数フレームへ分散する等のより大きな再設計)
    は指示なしに着手しない。
- **(2026-09-01、Round36-14 follow-up#16、完了済み)**: "ではボスの
  形態変化後の変更 現在はインフィニティ軌道でレーザー撃ってるが
  発射感覚が長すぎる 半分くらいに"に対応。`BOSS_BROKEN_BEAM_INTERVAL`
  を20→10へ半減。全回帰`run_all.py` **1237 passed/0 failed**。詳細は
  HANDOFF.md Round36-14(follow-up#16)参照。
- **(2026-09-01、Round36-14 follow-up#17、完了済み)**: "ステージ2の
  ボスの形態変化後のササピービームでインフィニティ軌道で停止の間隔が
  長いんで半分に これ以前に指示したが変更されてなかった あとサンダー
  ビームのSEが1回のみだが 発射中はSEをループ 終わったら当然音も停止"
  に対応。(1) follow-up#16の`BOSS_BROKEN_BEAM_INTERVAL`(ビーム発射
  間隔)とは別物の`BOSS_BROKEN_LAP_STEPS_MIN/_RANGE`(軌道の移動→停止
  サイクルにおける停止と停止の間隔)を48/32→24/16へ半減(過去の記録に
  この指示自体は見当たらず、今回が実質初対応)。(2) "サンダービーム"
  =SOUND_SBEAM(STAGE_SBEAM、ドロップ→スイープ→リトラクト攻撃)のSEを
  `UPDATE_SBEAM`の毎フレーム末尾でSBEAM_ACT!=0の間再着火するループ
  方式に変更、新規`STOP_SBEAM_SOUND`(SND_TIMER=0+即時R8ボリューム
  ゼロ書き込み)をSBEAM_ACTが0になる全経路(自然終了+ボスポーズ強制
  終了、後者は他ポーズの音を誤って止めないようSBEAM_ACT実際に非ゼロ
  だった場合のみ)に追加。新規テスト7件(`boss_broken_form_test.py`に
  実数値ピン留め2件、`sbeam_test.py`にループ/即時停止検証5件)。全回帰
  `run_all.py` **1244 passed/0 failed**。Comb ROM再ビルド・
  `verify_comb.py`で健全性確認、送付。詳細はHANDOFF.md Round36-14
  (follow-up#17)参照。
- **(2026-09-01、Round37、完了済み)**: Stage2側の作業に続き、初めて
  Stage1本体(`src/CYBER SHMUP.asm`)へ機能追加。ユーザーの「エネミー7」
  「E1」呼称がコード上どのシンボルに対応するかAskUserQuestionで確認、
  **両方ともコード上の同一個体`TYPE_ENEMY4`(BEHAVIOR_SIMPLE_DRIFT_
  DODGE)を指す**と判明(コード自身のコメントがこの対応を裏付け)。
  ユーザーから明示的指摘: "今の動作が意図したもの コメントがあるなら
  それはまちがい 整合性のために動作の変更はするな" - 調査中に見つけた
  コメントと実動作の食い違いは実動作を正として一切変更しないよう
  指示された。(1) Enemy7(TYPE_ENEMY4)の描画色をSPR_BLACKから新規
  `SPR_LIGHTGREEN`(03h)へ。ENEMY_TYPE_TABLE側の未使用な色バイトは
  「整合性のための変更はするな」を踏まえあえて触れず。(2) 汎用敵弾
  システムを新規実装(`EBULLET_POOL`、6スロット、hwスプライト) -
  Stage2の`flyerlaser_gen.py`が生成する8x8横棒ビットマップを流用しつつ、
  Stage1は完全に別バンク・別ビルドのためバイトデータ自体は新規定義
  (`EBULLET_PATTERN`)。カラーは新規`SPR_LIGHTRED`(09h)、パターン
  コードは事前調査済みの空き範囲(124-127)を使用。(3) Enemy7は
  `PLAYERY`と`(IX+E_Y)`が一致したら発射(再発射クールダウン付き、
  TYPE_ENEMY4が実質未使用と確認済みの`E_PARAM3`を再利用)。
  (4) E1(=Enemy7と同一個体)/E2(編隊A/B)/E5(TYPE_ENEMY1_LOOK)は
  共有`RANDOM_3_5`+`DECIDE_FIRE_SHOOTER`(何機おきに次が射手か、
  カウントダウン方式)で「ランダムに3〜5機に一度」を実装、各々の
  「唯一の斜め移動フェーズ」(E1=ドッジ発動瞬間、E2=退出ダイブ
  phase0、E5=離散フェーズが存在しないためEnemy1と同じENEMY_CENTER_X
  を代替トリガーとして採用)で1回だけ発射。E2のECS_S7_A/Bで挿入
  コードにより`JR`の分岐範囲(±127バイト)を超過したため`JP`へ変更
  (挙動は同一)。新規`tools/verify_enemy_bullets.py`(34件)で検証、
  既存の`tools/verify_*.py`群を横断的に再実行し退行なしを確認
  (`verify_idcache_multiframe.py`/`verify_namebuf_regen.py`の
  `KeyError: 'ROWDATA1'`は今回の変更と無関係な既存の問題とgit stash
  で確認済み、`verify_enemy_pool_scan.py`のミスマッチは新規発射
  ロジック追加により当然発生する想定通りの差分でバグではない)。
  `verify_vdp_wait_shrink.py`のOUT件数固定期待値のみ290→294・
  298→304へ更新。Comb ROM再ビルド・`verify_comb.py`でバンク切替
  健全性確認、ユーザー明示指示("今回はComb Romもくれ")によりComb
  ROM送付。詳細はHANDOFF.md Round37参照。
- **(2026-09-01、Round37 follow-up、完了済み)**: **Stage1エネミーの
  呼称整理(以後この呼称を使うこと、コード変更ではない)**:
  **Fighter=TYPE_ENEMY4(=前Roundで確認済みのEnemy7/E1と同一個体、
  直進+一度だけの斜めドッジ)、Zigzag=Enemy2(A/B編隊)、Wave=
  TYPE_ENEMY1_LOOK(サイン波ドリフト)**。ユーザーからの「E2が3つ
  あるのはなんだ?」には、`tools/schedule-editor.html`のコメントで
  発見した過去の統合履歴(Enemy2は元々e2a_top/e2a_bot/e2b_top/
  e2b_bot の4分割だったが現在は1つに統合済み)を回答しつつ、実際に
  見えている「3」はEnemy2自身が常に3機編隊(U0/U1/U2)である仕様の
  可能性が高いと回答。「ジグザグは斜め移動中に弾が出てない」の
  バグ報告は、生のStage1・Comb用パッチ済みビルドの両方で実際に
  MAINLOOPを回してstate7(Z字退出)開始時の発射を直接トレース
  検証したが再現できず(強制射手選定時は毎回発射、通常のランダム
  抽選でも15スポーン中3回正しく発射、発射時のスプライトスロット
  使用率も4-11/30と余裕あり) - 元々「3〜5機に1回」という仕様通り
  である可能性が高い旨と追加情報を依頼。Wave(TYPE_ENEMY1_LOOK)は
  "サインウェーブの頂点と下限で16px左にドリフトするよう変更"に
  対応 - 当初Enemy1(Fighter)のドッジと同じ「16フレーム1px/frame」
  ペーシングで実装したところ、頂点↔下限の間隔がちょうど16state分
  のため2つのダッシュが継ぎ目なく連続し「常時+1px/frameの恒久的な
  速度変化」になる副作用をE_Xの直接トレースで発見・訂正、「8フレーム
  で2px/frame追加(計16px)」方式(ダッシュ5px/frame⇔巡航3px/frameが
  交互に現れる視認可能なアクセント)に変更。`tools/verify_enemy_
  bullets.py`に3件追加(34→37件)、既存検証群も無退行。詳細は
  HANDOFF.md Round37 follow-up参照。
- **(2026-09-01、Round37 follow-up2、完了済み)**: "多分3編隊で1機
  としてカウントしているからだろう"でE2が3つ見える件は解決(前回の
  仮説を了承)。**Wave動作の訂正**: 直前のfollow-upで実装した「頂点/
  下限での追加ドリフト重ね掛け」方式は意図と異なると判明。正しい
  仕様: "サインの頂点と下限ではLut参照を停止して横に16px動き上下の
  動きは無くすということ つまりサイン移動で頂点まで行き16pxドリフト
  その後サイン移動で下限まで行き16pxドリフト この繰り返し"。
  `EBSB_UPDATE`を全面的に書き直し、`E_STATE`が頂点(state7)/下限
  (state23)プラトー開始に達した瞬間`E_PARAM4`に16をセットして
  凍結モードへ - 凍結中は`E_STATE`を一切進めず(Y完全静止)、Xのみ
  1px/frameで16フレームかけて移動、その後同じstateからサイン追従を
  再開。E_X/E_STATE/Yを実際にコール単位でトレースし仕様通りの挙動
  (頂点で16フレームの真の凍結区間、Y完全一定→通常再開→下限で同様)
  を直接確認。`verify_enemy_bullets.py`の該当テストを新仕様に合わせ
  全面書き直し(開発中にテスト自身のインデックスずれによる誤検知を
  発見・修正)、全40件PASS。既存検証群も無退行。詳細はHANDOFF.md
  Round37 follow-up2参照。
- **(2026-09-01、Round37 follow-up3、完了済み)**: (1) 自機の2枚目
  スプライト(ACCENT_MID_PATTERN、SPR_WHITE)を`tools/sprite-editor.
  html`互換のJSONへ変換して送付(自機前方バリア用のドット絵作成の
  ため)。(2) Fighter(TYPE_ENEMY4)にユーザー添付の2枚目ポーズ
  (`PAT_ENEMY4_2`、コード92-95)を追加し、ドッジ中のみ`E4_ANIM_
  FRAME_LEN`(4フレーム)毎に交互表示するアニメーションを実装。
  (3) "Eは一度上下移動に入ったらそのまま通常のドリフトには戻さず
  移動して消えるように"に対応 - 従来16px(`ENEMY_DODGE_DIST`)で
  打ち切っていた斜めドッジを、発動後は画面外に消えるまで永久に
  斜め移動し続けるよう変更(`EBSD_DIAG_SKIP_TRIGGER`をTYPE_ENEMY4か
  で早期分岐、新規`EBSD_DIAG_E4`ブロック)。開発中に`EBSD_DIAG_E4`が
  `E_PARAM0`(ドッジ発動済みか)を確認せず常時アニメ処理していた
  実バグをテストで検出・修正。`tools/verify_enemy_bullets.py`に
  5件追加(40→45件)、既存検証群も無退行。Comb ROM再ビルド・検証・
  送付(以後毎回自動送付方針、下記参照)。詳細はHANDOFF.md Round37
  follow-up3参照。
- **(2026-09-01、Round37 follow-up4、完了済み)**: "ウェーブのキャラが
  壊れてるな 2機一組で設計してあるが 1機たおされたあとのキャラパターン
  が壊れてる さっきの動作変更で壊したな"というバグ報告に対応。根本原因は
  follow-up2のWave頂点/下限LUTフリーズ機構が、フリーズ残りフレーム数を
  `E_PARAM4`に書き込んでいたこと - `E_PARAM4`は`SIMPLE_REDRAW`
  (象限撃破時のVRAMパターン再描画、BEHAVIOR横断で共有)が「0-3範囲の
  シーケンスindex」として読む既存の共有フィールドだったため、フリーズ
  中の撃破で範囲外の値がそのままindexに使われパターン破損を引き起こして
  いた。2ラウンド前の自分自身のコードコメント("E_PARAM4はこのBEHAVIORで
  未使用")が誤りだったと判明。修正: フリーズカウントダウンを`E_PARAM5`
  (Fighter側でのみ使用、Wave側からは無参照と確認済み)へ移動。同時に
  "ファイターのアニメは上下移動に入ったら戻さない 今は繰り返しになって
  るな"に対応し、Fighterのポーズ切替を無限トグルから「E4_ANIM_FRAME_LEN
  経過後に1回だけ切り替え、以後永久にそのまま」の一発切替方式へ変更。
  `verify_enemy_bullets.py`にE_PARAM5参照への更新+回帰防止テスト2件を
  追加、全49件PASS(45→49件)。既存検証群も無退行。Comb ROM再ビルド・
  検証・送付。詳細はHANDOFF.md Round37 follow-up4参照。
- **(2026-09-01、Round37 follow-up5、完了済み)**: 自機バリア装備を実装。
  "装備中はどちらのパターンにも追加 右下の8x8ドットのエリアがバリアの
  絵だからそれを未提出のアクセントに追記"に対応 - アップロード済み
  `Acsent_16x16.json`の右下8x8のみが実際の絵柄と判明、既存の
  `ACCENT_MID_PATTERN`/`ACCENT_DOWN_PATTERN`(両方ともBR象限が元々
  空白)のBR象限だけをこの絵に差し替えた新規`ACCENT_MID_BARRIER_
  PATTERN`/`ACCENT_DOWN_BARRIER_PATTERN`を追加。VRAM予算調査では
  シンボル名検索だけでコード96-111を空きと誤認しかけたが(実際は
  `BOSS_SPAWN`がリテラル数値で遅延ロードするボス専用パターンで
  使用済み)、全LDIRVM呼び出し元をリテラル込みで横断的に再チェック+
  エミュレータでの実VRAM実測の二重検証でコード128-255が真に空きと
  確認、128/132を新規使用。`BARRIER_HP`(0F1F1h、耐久値5、EQU
  `BARRIER_HP_INIT`)をINITで5にセットし初期状態で装備、既存の
  アクセント選択ロジック(JOY_STICKによるMID/DOWN分岐)を拡張し
  `BARRIER_HP>0`ならバリア版パターンを選択、0なら通常アクセントに
  復帰。ダメージ/衝突/ゲームオーバーは指示通り一切未実装(現状Stage1
  に自機被弾判定自体が存在しない)。新規`tools/verify_barrier.py`
  (14件、VRAM内容をアップロードJSONから独立に再導出して照合、実
  MAINLOOP1フレームでの表示切替も検証)、既存検証群も無退行。Comb
  ROM再ビルド・検証・送付。詳細はHANDOFF.md Round37 follow-up5参照。
- **(2026-09-01、Round37 follow-up6、完了済み)**: "自機へのダメージ
  判定はまだ実装してないのか 敵や敵弾に当たるとバリア耐久値1減少
  なくなって当たれば終了"に対応。着手前にAskUserQuestionで(1)判定
  対象範囲=現在実装済みの全ての敵・敵弾(Fighter/Wave/E2/EBULLETに
  加えEnemy3地上敵・Enemy6回転グリフ敵・ボス8ポッド本体+volley、
  ボスの偏向弾DFLのみ除外)、(2)バリア0被弾時の「終了」=完全停止
  (フリーズ)+爆発演出、の2点を確認した上で実装。自機を16x16固定
  ヒットボックスとして扱う新規`PLAYER_HIT_BOX8/16`(既存`QUAD_HIT_
  TEST`は片方の箱が*8グリッド前提のため自機の自由ピクセル座標には
  再利用不可、同じ4辺比較の形を踏襲)+敵システムごとの`PDC_CHECK_*`
  スキャナ6種を新設。接触した敵本体は破壊されない(体当たりでは
  死なない設計)が敵弾は命中時に消費。ヒット時はバリア残量>0なら
  HP-1+60フレームの無敵時間(未調整の暫定値)、残量0での被弾は
  `GAME_OVER`フラグを立て自機位置に爆発を1回発火。「完全に停止」は、
  このROMがper-frame HALTを使わないfree-running設計であることを
  活かし、MAINLOOP先頭に`GAME_OVER`チェックを1箇所追加するだけで
  実現(立っていれば本体を丸ごとスキップして即座に先頭へ戻る、
  VDP書き込みゼロで見た目はそのフレームのまま静止)。新規RAM
  (`GAME_OVER`/`BARRIER_IFRAMES`)はfollow-up#14の「RAM初期化漏れ」
  教訓を踏まえ最初からINITで明示ゼロクリア。新規`tools/verify_
  player_damage.py`(41件)、既存検証群も無退行(`verify_vdp_wait_
  shrink.py`のOUT件数固定期待値のみ294/304→296/306へ更新)。Comb
  ROM再ビルド・検証・送付。詳細はHANDOFF.md Round37 follow-up6参照。
- **(2026-09-01、Round37 follow-up7、実機フィードバック対応・
  完了済み)**: follow-up6への5点フィードバックに対応。(1)自機
  ヒットボックスを16x16→下側左8x8(PLAYERX,PLAYERY)-(+7,+7)に縮小
  (`PDC_CHECK_PODS`だけ`PLAYER_HIT_BOX8/16`を経由しない独自実装
  だったため個別修正が必要と判明)。(2)バリア吸収時のヒット
  エフェクトを爆発→カラーチェンジ(ホワイト→パープル、`BARRIER_
  IFRAMES`連動)+新規サウンド(`SOUND_BARRIER_HIT`、低音・8フレーム
  中2フレームのみ最大音量=25%duty)に変更。(3)バリア枯渇後の被弾を
  BG式`TRIGGER_EXPLOSION`ではなく新規16x16スプライト爆発バースト
  (`PLAYER_EXPL_POOL`、自機起点±8pxランダム、白/赤点滅、約2秒)に
  変更。(4)GAME_OVERフラグの記録は残しつつ、前回追加したMAINLOOP
  凍結ゲートを完全削除(検証できないため)。(5)Fighter2枚目ポーズを
  新添付データ(`E42_16x16.json`)へ差し替え。**開発中に3件の実バグを
  自己発見・修正**: `SOUND_UPDATE_B`→`SOUND_UPDATE_C`の暗黙フォール
  スルーの間に新規関数を割り込ませて破壊しかけた回帰、`ALLOC_SPRITE_
  NUM`呼び出し前にHLをIXへ退避し忘れてスポーンが機能しなかった回帰
  (SPAWN_EBULLETと同じ既知の罠)、そして最重要: 新規RAM
  (`PLAYER_ACCENT_COLOR`〜`PLAYER_EXPL_POOL`)が、`src/CYBER SHMUP.
  asm`自身のコメントには一切現れない`tools/bankswitch_poc/build_
  full_rom.py`(Comb限定)のバンク切替トランポリン実行時コピー先
  (0xF200-0xF201)を侵食し、`verify_comb.py`のボス撃破後バンク切替が
  無限ループする形で発覚(生Stage1単体テストでは無症状)。新規RAM群を
  0xF210〜へ後方シフトし、該当EQU直前に今後同じ罠を踏まないための
  警告コメントを追加して解消。`tools/verify_player_damage.py`を
  大幅書き換え、全55件PASS。既存検証群も無退行(`verify_vdp_wait_
  shrink.py`のOUT件数のみ296/306→300/312へ更新)。Comb ROM再ビルド・
  `verify_comb.py`で健全性確認、送付。詳細はHANDOFF.md Round37
  follow-up7参照。
- **(2026-09-01、Round36-14 follow-up#18、完了済み)**: "ではボス登場前
  に中身が空の透明のスプライトを4枚横に並べて ボス登場Y位置の上16px
  からボス表示外の64pxまで高速移動 ボスが攻撃に入ったら消す こうする
  事でボスを16px幅で消すことが出来るんで登場演出に Combではなく
  Stage2のRomで Combはテストに時間がかかるんで"に対応。TMS9918実機の
  「1走査線につき実際に描画されるのは属性テーブル先頭から最大4枚のみ」
  という仕様を利用し、ボス本体(`BOSS_SPR_BASE_SLOT=10`)より若い
  スロットに透明(color=0)ダミー4枚(`BOSS_WIPE_SPR_BASE_SLOT=4`、
  `HORMING_SPR_BASE_SLOT`と同じ「ボススポーン時点で構造的に空」な
  ENEMY_SPR_BASE_SLOT3枠+BULLET_U_SPR_BASE_SLOT1枠を再利用)を配置、
  `BOSS_SPAWN_Y-16`から`BOSS_SPAWN_Y+64+64`までY方向に高速(4px/frame、
  未調整の仮値)スイープさせ、ボス本体を16px幅×4本の帯で見かけ上
  消去する演出を実装(`S2_BOSS_SPAWN`起点で発火、最初の攻撃ポーズ
  `UBA_MOVE_RIGHT`で安全弁として強制非表示)。開発中に`stack_safety_
  test.py`(STACKTOPまでの安全マージン)がFAILし、このファイルの
  RAM安全マージンが本ラウンド着手前から既にゼロ(新規RAM確保の余地が
  全く無い状態)だったと判明、新規`BOSS_WIPE_ACT`/`BOSS_WIPE_Y`を
  ボス死亡演出専用の`BOSS_EXPL_CX`/`BOSS_EXPL_CY`へエイリアス(時間的
  排他が保証済み)、16バイトのステージングバッファも完全に廃止して
  新規RAM確保ゼロで解消。`init_ram_poison_test.py`のTier B救済判定
  にシンボル別期待値の上書き機構を追加(`BOSS_WIPE_ACT`は実スポーン
  時点で1が正、0固定という旧来の前提を拡張)。新規`tools/stage2_
  combined/tests/boss_wipe_test.py`(19件)、`vdp_wait_test.py`の
  ハードコードOUT件数更新(完全展開ルーチンのため99h側+2/98h側+16と
  他の追加パターンと異なる特殊な増分)。全回帰`run_all.py`
  **1267 passed/0 failed**(1244→1267)。**ユーザーの明示指示により
  今回はComb ROM再ビルド・送付を行わずStage2単体ROMのみ送付**。
  詳細はHANDOFF.md Round36-14 follow-up#18参照。
  - **保留**: `BOSS_WIPE_SPEED`(4px/frame)は未調整の仮値。見た目の
    実機/エミュレータでの視覚確認はまだ行っていない(送付ROM自体での
    確認待ち)。
- **(2026-09-02、Round36-14 follow-up#19、完了済み)**: "1回だけではなく
  攻撃に移るまで継続してループだぞ で、もっと上下移動を早く 8px単位で
  全速で 現在はウェイト入れてるのか"に対応。(1) `UPDATE_BOSS_WIPE`を
  「`BOSS_WIPE_END_Y`に達したら非活性化して停止」から「達したら
  `BOSS_WIPE_START_Y`へ巻き戻して回り続ける」永久ループへ書き換え。
  従来の停止処理を新設`STOP_BOSS_WIPE`として独立させ、ボスの最初の
  攻撃ポーズ開始地点`UBA_MOVE_RIGHT`からの呼び出しを「見た目を消す
  だけ」から「`BOSS_WIPE_ACT`も同時にクリア」へ変更(このループを
  止める唯一の経路になったため、ACTを残すと次フレームで即座に
  再描画されてしまう)。(2) `BOSS_WIPE_SPEED`を4→8px/frameへ倍増
  (1周36フレーム→18フレーム)。(3) 明示的なウェイト/フレームスキップは
  元々存在せず、`UPDATE_BOSS_WIPE`は他の全ボスサブシステムと同じく
  毎フレーム無条件に呼ばれている旨を回答。`boss_wipe_test.py`を新設計
  に合わせ全面改訂(19→24件、3周連続ループの直接検証・`STOP_BOSS_WIPE`
  単体検証・停止後の非復活検証を追加)。RAM配置は今回変更なしのため
  `stack_safety_test.py`/`init_ram_poison_test.py`は無変更で通過。
  全回帰`run_all.py` **1272 passed/0 failed**(1267→1272)。前回の
  "Combではなく"は今回にまで及ぶ恒久指示ではないと判断し、CLAUDE.md
  記載の標準方針(コード変更の一区切りごとにComb ROM自動送付)へ復帰、
  `verify_comb.py`で健全性確認の上でStage2単体ROM・Comb ROM両方送付。
  詳細はHANDOFF.md Round36-14 follow-up#19参照。
  - **保留**: `BOSS_WIPE_SPEED`(8px/frame)は依然"全速"からの未調整な
    仮値、実機/エミュレータでの視覚確認待ち。永久ループ化により
    ボスが長時間攻撃に入らない場合ワイプが延々と回り続ける見た目に
    なるが、これは今回の指示通りの意図した挙動。
- **(2026-09-05、Round36-14 follow-up#20、完了済み)**: "まず爆発の
  キャラが消えてる 恐らくブランクで書き換えたな でブランクのワイプの
  移動速度が遅い 次にワイプ中は初期停止状態のスプライトでワイプが
  終わるまで停止すること"に対応。(1) "爆発のキャラが消えてる"の実
  バグを特定・修正: `BOSS_WIPE_ACT`/`BOSS_WIPE_Y`の`BOSS_EXPL_CX`/
  `BOSS_EXPL_CY`エイリアス(follow-up#18のRAM安全マージン制約による
  苦肉の策)が、follow-up#19の「攻撃に移るまで継続ループ」化で
  ワイプの実効時間が延びたことにより、"ワイプがまだ動いている間に
  ボスへ十分なダメージが入りSPARK爆発が発火する"という、前提として
  いた時間的排他が崩れるケースが現実に起こりうると判明。ワイプの
  ピクセルY値がBG演出のセル行として誤読され爆発が範囲外に描画される
  (見た目上消える)。2段修正: (a)`TRIGGER_BOSS_BROKEN_FORM`/
  `INIT_BOSS_EXPLOSION`冒頭に`CALL STOP_BOSS_WIPE`を追加。(b)それ
  だけでは不十分(`UPDATE_BOSS_WIPE`は`BOSS_FORM`を見ておらず、(a)の
  直後に本物のセル座標が書き込まれると次フレームで「再アクティブ化」
  と誤読し続けてしまう)と実際にレンダリングで発見、`UPDATE_BOSS_WIPE`
  自身に`BOSS_FORM!=0`即RETゲートを追加(`UPDATE_BOSS_ALL`と同じ
  一方向ゲート変数を流用、個々の呼び出し元の実装漏れに依存しない
  構造的解決)。ゲートを一時的に外して新規テストが実際に落ちることを
  確認してから復元、回帰ガードの実効性も検証済み。(2)
  `BOSS_WIPE_SPEED`を8→16px/frameへ再倍増。(3) "ワイプ中は初期停止
  状態のスプライトでワイプが終わるまで停止すること"に対応し
  `UBA_ACTIVE`の先頭に`BOSS_WIPE_ACT!=0`即RETゲートを追加(ワイプ中
  ボスは`S2_BOSS_SPAWN`の初期状態のまま完全静止)。この変更により
  follow-up#19の「攻撃ポーズ到達で停止」という終了条件が到達不能
  (デッドロック)になるため、`BOSS_WIPE_ACT`を0/1フラグから残り
  ラップ数のカウントダウンへ再設計(新設`BOSS_WIPE_LAPS`(4)を
  `TRIGGER_BOSS_WIPE`がシード、`UPDATE_BOSS_WIPE`がラップ完了ごとに
  1減算し0で自律停止) - follow-up#19の「1回だけではなく継続して
  ループ」を保ったまま、ボスの状態に一切依存せず自律的に終了できる
  設計に変更。`boss_wipe_test.py`を全面改訂(32→34件)、無関係な
  既存テスト`boss_pose_test.py`/`boss_test.py`/`thunder_test.py`は
  スポーン直後に`BOSS_WIPE_ACT=0`を直接セットしてワイプを迂回する
  よう更新(意図した挙動変更に伴う前提更新、バグではない)。
  `init_ram_poison_test.py`のTier B期待値もシンボル参照化。VRAM→PNG
  レンダリングでワイプ有無に関わらずSPARK演出が同一の見た目になる
  ことを確認(修正済み)。全回帰`run_all.py` **1282 passed/0 failed**
  (1272→1282)。Comb ROM再ビルド・`verify_comb.py`で健全性確認の上、
  Stage2単体ROM・Comb ROM両方送付。詳細はHANDOFF.md Round36-14
  follow-up#20参照。
  - **保留**: `BOSS_WIPE_SPEED`(16px/frame)・`BOSS_WIPE_LAPS`(4)は
    いずれも未調整の仮値、実機での見え方次第で再調整の可能性あり。
- **(2026-09-05、Round36-14 follow-up#21、完了済み)**: "ボス出現時は
  5秒停止しワイプを繰り返すように変更"に対応。follow-up#20の「残り
  ラップ数(4)をカウントダウン」という間接的な停止条件を、「ボス出現
  から5秒間」という明示的な実時間ベースの停止条件に再設計。ボスは
  スケジュール駆動で必ず固定の`BOSS_SPAWN_TICK`(995)にしか出現しない
  ことを利用し、終了時刻`BOSS_WIPE_END_TICK EQU BOSS_SPAWN_TICK+
  BOSS_WIPE_DURATION_TICKS`をコンパイル時定数として計算、既存の
  グローバル16bit`GAME_TICK`と毎フレーム比較(`BOSS_POSE_END_TICK`等と
  同じ`SBC HL,DE`方式)する設計に変更 - 新規RAMを1バイトも使わずに
  実時間ベースの終了判定を実現し、`BOSS_WIPE_ACT`もfollow-up#18/#19
  当初と同じ単純な0/1フラグに戻せた。**開発中に自分の誤った前提による
  実バグを発見・修正**: 当初`BOSS_WIPE_DURATION_TICKS=300`(60fps前提を
  そのままGAME_TICKの単位に誤って適用)で実装したが、新規の実MAINLOOP
  直接計測テストが「300フレーム進めてもGAME_TICKが37前後しか進んで
  いない」という8倍のズレを検出 - `GAME_TICK`は実は生フレームカウンタ
  ではなく、別の`TICK`変数(毎フレーム無条件)が真の生フレームカウンタで、
  `GAME_TICK`自体は`AND 07h`ゲートにより8生フレームに1回しか進まない
  (スケジュールエディタの単位に合わせるため)とMAINLOOP自身のコメントで
  確認、正しい換算(5秒=300生フレーム÷8=37.5、切り上げ38)に修正。この
  バグは単体テストだけでは検出できず、`step_frame()`で生フレームを
  1個ずつ進める統合テストがあったからこそ発見できた。`boss_wipe_
  test.py`を新設計に合わせ全面改訂(34→38件)。全回帰`run_all.py`
  **1286 passed/0 failed**(1282→1286)。Comb ROM再ビルド・
  `verify_comb.py`で健全性確認の上、Stage2単体ROM・Comb ROM両方送付。
  詳細はHANDOFF.md Round36-14 follow-up#21参照。
  - **保留**: `BOSS_WIPE_DURATION_TICKS`(38、"5秒"からの計算値だが
    切り上げにより実際は約5.07秒)・`BOSS_WIPE_SPEED`(16px/frame)は
    実機での見え方次第で再調整の可能性あり。
- **(2026-09-05、Round36-14 follow-up#22、完了済み)**: ボス出現音の
  Web Audio試聴でユーザーが選んだ候補G2について"G2でオクターブ下げて"
  →"おｋ 音はそれで実装してくれ"で音の実装を承認、同じメッセージで
  続けて"ではワイプ処理はやめる 変わりに出現時の初期位置は今のままで
  1フレームごとに左端、右端から交互に表示位置を変えながら中央まで
  繰り返す 点滅しながら中央で実態化みたいな演出 その間5秒として
  中央への移動量を割り出してくれ その後初期位置までまた戻って攻撃に
  移る"の指示でワイプ演出(follow-up#18-#21)を全面撤回・差し替え。
  `BOSS_WIPE_*`一式を完全削除し、ボス本体自身を毎フレーム左候補/
  右候補(TICK最下位ビットで交互、両方とも中央`BOSS_SPAWNX/2`へ収束)
  へ再描画するだけの新規hwスプライト消費ゼロの点滅演出に置き換え
  (`BOSS_MATERIALIZE_*`)。乗除算命令の無いアセンブラのため
  `STEP_PX=3`×`TICKS=32`=96px(振幅と一致・余りゼロ)を選び、
  約4.27秒という意図的な近似値を採用(厳密な5.0秒ではない旨コード内に
  明記)。収束完了後は`BOSS_SPEED`でのフリッカー無し滑走で`BOSS_SPAWNX`
  へ復帰しパトロールへ引き継ぎ。ワイプ機能とfollow-up#20由来のRAM
  衝突バグ修正パターン(`BOSS_EXPL_CX/CY`エイリアス+`STOP_*`呼び出し
  +`BOSS_FORM!=0`ゲートの2重防御)をそのまま新演出にも最初から適用。
  ボス出現音`SOUND_BOSS_MATERIALIZE`はG2(周期380)を実装 - 380>255の
  ためこのファイルで初めてPSGトーン周期レジスタ1(上位4bit)に非ゼロ
  値を書き込む実装、envelopeは`SOUND_THUNDER`/`SOUND_SBEAM`と同じ
  共有チャンネルAのboom方式+デューティゲートを再利用(新規エンジン
  コード無し)。**新規回帰テストの自己検証中に、テスト自体の欠陥を
  発見・修正**: `BOSS_FORM!=0`ゲートを一時的に外して再実行しても
  37件全てPASSしてしまう(ゲート欠落を検出できない)という不備を
  発見、原因はテストシナリオが`BOSS_X`をスポーン直後の値のまま
  トリガーしていたため偶然の自己補正が起きていたこと - `BOSS_X`を
  真に収束途中の値(50)にしてから発火する形にテストを修正し、今度は
  ゲート削除時に正しく1件FAILすることを確認、ゲートを復元して
  最終的に全件PASSに戻ることを確認済み。全回帰`run_all.py`
  **1285 passed/0 failed**。Comb ROM再ビルド・`verify_comb.py`で
  健全性確認の上、Stage2単体ROM・Comb ROM両方送付。詳細はHANDOFF.md
  Round36-14 follow-up#22参照。
  - **保留**: `BOSS_MATERIALIZE_STEP_PX`(3px/game-tick)・
    `BOSS_MATERIALIZE_TICKS`(32、≒4.27秒)・`BOSS_MATERIALIZE_SND_
    RETRIGGER`(96生フレーム≒1.6秒)はいずれも未調整の初期値、実機
    での見え方・音の聞こえ方次第で再調整の可能性あり。
- **(2026-09-05、Round36-14 follow-up#23、完了済み)**: "まずマテリア
  ライズ中はボスコリジョン無効\nその後初期位置に戻ったら現在は左に
  行って往復するが往復はせず即攻撃に移るように"に対応。(1)
  `CHECK_HIT_PAIR_BOSS`に`BOSS_MATERIALIZE_ACT!=0`なら即RETするガード
  を追加、マテリアライズ中(収束・復帰いずれも)は被弾しない。(2)
  復帰完了時に通常の左端パトロールを経由せず即攻撃ポーズへ入るよう、
  `UBA_MOVE_RIGHT`の右端到達時の「攻撃ポーズ突入」処理を共有サブ
  ルーチン`ENTER_BOSS_ATTACK_POSE`へ切り出し、`UBM_RETURNING`(復帰
  完了)からもここへ直接ジャンプする設計に変更。**開発中に重大な
  自己発見バグ**: (1)の新規ガードが`UPDATE_BOSS_MATERIALIZE`自身の
  `BOSS_FORM!=0`ガードを踏襲していなかったため、`TRIGGER_BOSS_
  BROKEN_FORM`がHP<=100到達でBOSS_FORMを1にした直後、`BOSS_
  MATERIALIZE_ACT`のエイリアス先`BOSS_EXPL_CX`に書き込まれる本物の
  セル座標(ほぼ確実に非ゼロ)を「実体化中」と誤読し、**以後ボスへの
  被弾が永久に成立しなくなる**(実質無敵化)重大バグを`boss_
  collision_test.py`/`boss_broken_form_test.py`の実HPドレイン統合
  テストの無限ハングから発見・修正(`CHECK_HIT_PAIR_BOSS`にも
  `BOSS_FORM==0`の間だけ判定する同じガードを追加)。両テストファイル
  にスポーン直後の実体化バイパスも追加。`boss_materialize_test.py`に
  5件追加(49→51件、上記重大バグの回帰ガード含む)、全ての新規ガード
  について修正一時除去→FAIL確認→復元→PASS確認の自己検証を実施済み。
  全回帰`run_all.py` **1299 passed/0 failed**。Comb ROM再ビルド・
  `verify_comb.py`で健全性確認の上、Stage2単体ROM・Comb ROM両方
  送付。詳細はHANDOFF.md Round36-14 follow-up#23参照。
  - **保留**: 実機での見え方(マテリアライズ中の無敵状態が違和感を
    生まないか)はフィードバック待ち。
  - **(2026-09-05、ユーザー確認)** "了解 引き続き頼む ボス関連は
    問題なし" - follow-up#23のボス関連変更(マテリアライズ中の無敵化・
    復帰後の即攻撃移行)は実機確認で問題無しとの最終確認済み。この件は
    一区切り、次の具体的指示は次回のユーザー発言待ち。

## Stage2 BGMドライバ実装(2026-09-05、Round38、完了済み)

- ユーザー指示: "ではBGMを実装する Stage1に関してはあとで実装するんで
  まずStage2から Vsyncを使ってBGMドライバを実装 で、ドライバ自体は
  各バンクに配置 もう空き容量がほとんど無いので BGMは空きの範囲で
  仮実装 間に合えばタイトルやEDも実装予定だが 今の残りROMでは入らない
  と予想されるんで新バンクで対応"。
- Stage2はStage1と異なりHALT+EIのvsync同期を使わない設計のため、
  「Vsyncを使ってBGMドライバを実装」は実MSXのH.TIMI(0FD9Fh)フックへ
  本物の割り込みハンドラとしてインストールする形で実現(`INIT_BGM`/
  `BGM_TICK`)。PSGチャンネルB/C(何ラウンドも前から予約済み・未使用
  だった)を使用、チャンネルAのSFXとはR7(ミキサー)のread-modify-
  writeで共存。
- **重要な副作用**: BGM_TICKという本物の割り込みハンドラが既存の
  PSGレジスタ選択機構(単一アドレス/データラッチ)と競合しうるため、
  既存SFX 12箇所(SOUND_SHOT等+SOUND_UPDATE/SU_BOOM)のPSG書き込み
  ペアを全てDI/EIで保護。
- 自己発見・修正したバグ: (1)`BGMT_LOAD_ROW`のレジスタ破壊(チャンネル
  Cの音程が壊れる)、(2)`z80emu.py`にPSGレジスタモデルが皆無だった点
  (新規`psg_latch`/`psg_regs`/`io_out`/`io_in`を追加)、(3)
  `stack_safety_test.py`がSTACKTOP以上のアドレス(新規`HTIMI_HOOK`)を
  誤って「最も近いゲーム変数」と誤認する潜在バグ、(4)
  `terrain_render_perf_test.py`独自テーブルリストへの`bgm_gen`追加漏れ
  (過去にも繰り返し発生している既知の構造的リスク)。
- スタック安全性: 新規の本物の割り込みハンドラ追加を受け、MAINLOOP
  単体の実測低水位マーク・最も近いゲーム変数との実マージン(73バイト)
  に対し、BGM_TICKワーストケース実測(16バイト)+BIOS側割り込み
  プロローグの保守的仮定(20バイト)=36バイトが収まることを新規
  検証・記録(`stack_safety_test.py`)。
- 新規`bgm_test.py`(50件)、全回帰`run_all.py` **1350 passed/0 failed**
  (1299→1350)。Stage2単体ROM(残り1778バイト)・Comb ROM再ビルド・
  `verify_comb.py`健全性確認の上、両ROM送付。詳細・技術的経緯は
  `tools/stage2_combined/HANDOFF.md`のRound38を参照。
  - **保留**: プレースホルダーの旋律・音量は仮実装(本格的な作曲は
    別途)。実機でのH.TIMIフック自体の動作確認(z80emuは本物の割り込み
    を発火しないため必須)は実機フィードバック待ち。Stage1側のBGM
    ドライバ実装は明示的にスコープ外(次回以降)。タイトル/ED用の
    新規バンクは今回未着手。

## Stage2スケジュール差し替え(2026-09-05、Round38-2、完了済み)

- ユーザー作成の新スケジュール`Schedule2_10.json`(placements166件+
  terrain492列)を機械的に統合。Round36-9と同じPythonスクリプト生成
  手法。terrain配列の物理的に不可能な段差2箇所(index316/454)を
  Round36-9と同じ基準で最小補正(ユーザー確認は求めず、ビルドを
  ブロックする制約のため)。`TERRAIN_TRACK_LEN`は532→608に変化。
- 全回帰`run_all.py` **1366 passed/0 failed**(1350→1366)。Stage2単体
  ROM(残り1218バイト)・Comb ROM再ビルド・送付済み。詳細は
  `tools/stage2_combined/HANDOFF.md`のRound38-2参照。

## 新バンク:タイトル画面バンクテスト(2026-09-05、Round39、完了済み)

- ユーザー指示: "ではバンクテストをしたいので 新バンクには必要な
  初期化処理を実装した上で PUSH STARTと表示しStage1とStage2のボスを
  適当に表示して ボタンが押されたらStage1へトランポリンするように"。
  併せて「RAMページ3(C000h-FFFFh)は8KBでなく実際は16KBリニアで使える」
  という仕様確認あり(BGMには影響なし)。
- Stage1・Stage2に続く**3つ目のバンクペア(タイトル画面)**を新規実装
  (`tools/title_screen/title_test.asm`+専用`build_test.py`)。ASCII16は
  window Aが常にbank0から起動するため、新規タイトルバンクをbank0/1に
  配置し、Stage1(旧0/1→2/3)・Stage2(旧2/3→4/5)を2バンクずつシフト。
  ボスの絵は新規に描かず、Stage1の`BOSS_PATTERNS`/`BOSS_MAP`とStage2の
  `sasapi_gen.py`のデータを実アセンブル結果から直接抜き出して再利用
  (`tools/title_screen/title_gen.py`)。テキストはBIOSデフォルトフォント
  をそのまま使用。
- 自己発見・修正したバグ3件: (1)Stage1側トランポリンのバンク番号
  シフト漏れ(重大、window Bが誤ったバンクのまま起動継続する)、
  (2)`title_test.asm`自身のアセンブラ演算子優先順位バグ(既知の
  "BASE+N*4"パターンの再発、テキストが一切描画されない原因)、
  (3)`z80emu.py`にWRTVRM(単一バイトVRAM書き込み)シミュレーションが
  無かった(新規追加、後方互換)。
- `tools/bankswitch_poc/verify_comb.py`を新6バンク構成用に全面改訂、
  title→Stage1→Stage2の一気通貫バンク切替を実際にエミュレータで検証、
  全チェックPASS。新規`tools/title_screen/title_test.py`(23件)。
  `run_all.py`全回帰**1366 passed/0 failed**(無変化)。Comb ROM総
  サイズは128KB維持(96KBの実データ+0xFF埋め予備2バンクでパディング、
  192KBへの単純倍化という未検証の選択より保守的な判断)。詳細は
  `tools/stage2_combined/HANDOFF.md`のRound39参照。
  - **保留**: Comb ROMの新レイアウト(128KB構成)自体が実機で正常に
    ブートするかは未検証。"PUSH START"の実機表示・両ボスの配置座標
    (170,40)/(row2,col2)は"適当に"の暫定値、実機フィードバック待ち。
    新バンクへのBGMノートデータ配置・RAM転送方式自体は今回は
    未着手(将来タスク)。

## 実MIDI楽曲のPSG再生・独立2chドライバへ全面再設計(2026-09-05、
Round40、完了済み)

- ユーザー指示(実機確認"良好 バンク切り替えも実機で問題なく動いた"に
  続けて): "では添付ファイルのMIDIをノートだけ抽出してPSGで鳴らして
  くれ どちらも2ch分のデータになってるがMIDIなのでサイズが大きい で、
  方法としては先ほどのRAM方式で転送 タイトル含めて各ステージに
  ドライバを配置しRAMにコピーしてステージスタート"(添付:
  `Alone_Fighter.mid`・`Defeat_.mid`)。
- 新設`tools/bgm_data/`: `midi_to_psg.py`(mido使用、MIDI→
  (note,duration)行データ変換、MIDI50-84直接キーの周期テーブル)、
  `bgm_bank_gen.py`(周期テーブル+両曲2ch分をComb ROM専用16KBバンクへ
  まとめる。**実行時はmido不要**-生成結果は`bgm_bank.bin`/
  `bgm_layout.json`としてgit管理されたキャッシュを読むだけ。
  再生成は`python3 tools/bgm_data/bgm_bank_gen.py --generate`)。
  曲の割り当て(ユーザー確認なしの暫定決定): **Stage2=DEFEAT、
  Title/Stage1=ALONE_FIGHTER**(2曲を3箇所で使うためTitle/Stage1が
  共有)。
- BGMドライバをRound38の「chB/chC共有1行・固定duration」方式から
  「各チャンネル独立ポインタ・タイマー」方式へ全面再設計(実MIDI
  2トラックは長さも音符の長さもバラバラなため)。
- **Title/Stage2は自分自身でwindowB一時切替+RAMコピー**(起動時に
  bgm-dataバンク[Comb内bank6、旧来の0xFFパディングを実データ化]を
  選択→周期テーブル+曲データをLDIRでRAM[既定0xC000起点]へ→自分の
  本来のバンクへ復帰)。`build_full_rom.py`に`STAGE2_BGM_BANKSELECT_
  ANCHOR/PATCH`・`TITLE_BGM_BANKSELECT_ANCHOR/PATCH`を追加し
  standalone番号(bgm-data=2)をComb用グローバル番号(=6)へパッチ。
  **Stage2固有の落とし穴(自己発見)**: 既定の0xC000起点は
  `combined_test.asm`が既に`SBEAM_SPRITE_ATTRS`等の実データで使い
  切っており全72件のテストが原因不明のFAILの雨になった - Stage2
  専用に実測で空きと確認済みの`STAGE2_DATA_BASE=0xC200`を新設して
  解消(教訓: 「ここは空いているはず」を過去のコメントで判断せず、
  必ずその時点のシンボルテーブルを実際にダンプして確認すること)。
- **Stage1は自分ではバンク切替もRAMコピーも一切行わない**(意図的判断、
  ユーザーへの確認は行わず): 単体版ROM廃止済み+`tools/verify_*.py`
  群がバンク概念の無いフラット64KBメモリ前提という2点のリスクを
  踏まえ、Stage1へトランポリンする直前に必ず起動されるTitleが
  一度だけコピーしたRAMを、Stage1のINIT_BGMはそのまま読むだけに
  した。
- **Stage1固有の課題(実装中に判明、ユーザー未確認で自己解決)**:
  Stage2と違いStage1は元々PSGの全3チャンネルを既存SFXで使い切って
  いた(channel Aはnoise専用でtone不可、channel B=SOUND_POD_FIRE、
  channel C=SOUND_SHOT/SOUND_POD_HIT/SOUND_BARRIER_HIT)。「SFXの
  タイマーが非0の間、BGM側は該当チャンネルへの書き込みを完全に譲る」
  優先方式で共存させ、反対側の`SOUND_UPDATE_B`/`SUC_NORMAL`も
  「タイマーが既に0なら何も書かない」よう合わせて修正(片方だけでは
  共存が成立しないため)。SFX自体の音量・減衰・ピッチは一切無変更
  (`tools/verify_stage1_bgm.py`で直接比較検証済み)。
- 新規`tools/verify_stage1_bgm.py`(43件)、`bgm_test.py`全面書き換え
  (61件)、`title_test.py`にBGM検証12件追加(35件)、
  `verify_comb.py`全面改訂(title→Stage1→Stage2一気通貫でRAM上の
  BGMデータをバイト単位で直接検証)。全回帰`run_all.py`
  **1377 passed/0 failed**。Stage2単体ROM(残り1339バイト)・
  Title単体ROM・Comb ROM(bank6が実データ16384バイトへ、総128KB
  維持)を再ビルド・`verify_comb.py`で確認の上、送付。詳細・技術的
  経緯は`tools/stage2_combined/HANDOFF.md`のRound40を参照。
  - **保留**: プレースホルダーでなく本物の楽曲になったことで、実機
    での聞こえ方(音量バランス・Stage1のSFXとの共存・ループの繋がり)
    は全て実機フィードバック待ち。曲の割り当て(Stage2=DEFEAT、
    Title/Stage1=ALONE_FIGHTER)はユーザー確認なしの暫定決定 -
    実機で聞いた結果次第で入れ替え等の変更があり得る。ED画面自体は
    引き続き未実装。

## Round40 実機フィードバック対応: BGM off-by-oneバグ修正(2026-09-05、
完了済み)

- ユーザー報告: "こりゃ酷い 音はなってるがピーピー不協和音 多分休符も
  無視してるな テンポも無茶苦茶だ 自分でドライバ実装してて仕様を
  一致させられんのかお前は"。
- **根本原因**: `BGMT_UB/UC_NEWROW`が新しい行を読み込んだそのtick
  自体で既に1tick分再生しているのに、durationをそのままTIMERへ
  格納していたため**どの行も意図した長さより必ず1tick長く**鳴って
  いた。BGM_TICKを2000回連続実行して観測されたPSG書き込みの実測列を
  本物の行データと突き合わせて確定 - chB/chCで行数が全く異なるため
  (DEFEATはchB150行/chC818行)累積ズレが2ch間で大きく食い違い、曲が
  進むほど不協和音化していたのが真因。単発の値チェックでは検出
  できないバグで、既存テストは全てPASSしていた。
- 修正: duration読み込み直後、TIMER格納前に`DEC A`を追加(3ファイル
  [Stage2/Title/Stage1]全てに同一パターンで存在、全て修正)。
- 回帰テスト: 既存の単発行チェック9箇所を`duration-1`へ更新、
  **今回のバグを実際に検出できる新規の「多tick通しシミュレーション」
  テスト**(BGM_TICK2000回連続実行→観測列と本物データの完全一致を
  検証)を`bgm_test.py`/`title_test.py`/`verify_stage1_bgm.py`に追加
  (修正前のコードで実際にFAILすることを確認済み)。全回帰
  `run_all.py` **1379 passed/0 failed**、他2ファイルも増分件数で
  全PASS。3ROM再ビルド・`verify_comb.py`確認の上、再送付。
  詳細はHANDOFF.md Round40(実機フィードバック対応)参照。
  - **教訓**: 独立進行する複数ストリームを同期させるドライバは、
    単発行の検証だけでは累積誤差型バグを検出できない - 今後は必ず
    多数tick分の通しシミュレーションで実データと突き合わせること。

## Round40 実機フィードバック対応その2: R7ミキサー定数の修正
(2026-09-05、完了済み)

- ユーザー指摘"恐らくステージ2のボススポーンのサウンドとBGM被ってる
  だろ ChAになってるか"に対応。チャンネル自体は正しくchannel Aだった
  が、`MIXER_NOISE_A`(0F7h)/`MIXER_TONE_A`(0FEh)というchannel A用の
  R7 blind-write定数がbits1-2(tone B/C enable)を"disabled"に固定して
  おり、この2定数を使う全10種のSFX(SOUND_SHOT等)が発火するたび
  BGMのtone B/Cを巻き込んで無効化していた(次のBGM_TICKで最大1
  フレーム後に復旧)。定数を0F1h/0F8hへ変更しbits1-2を常時enabledに
  固定、10ルーチン全ての発火後R7を検証する新規テスト10件を追加。
  全回帰`run_all.py` **1389 passed/0 failed**。Stage2単体ROM・
  Comb ROM再ビルド・送付(Titleは無変更)。詳細はHANDOFF.md
  Round40(実機フィードバック対応その2)参照。
- **保留(今回は実装せず、指示により明示的に見送り)**: 同じ発言で
  ユーザーから"RAMコピーは各ステージが自分の開始時に元のbgm-data
  バンクへ戻って自分の曲だけを転送すべき(そうしないとRAMに入る曲が
  短くなる)"という設計指摘があったが、"テストだからタイトルだけで
  いい 他いじってもトークンの無駄だ"と明示されたため、**Stage1/
  Stage2側のRAMコピー方式変更は今回未着手**。現状は「TitleがStage1
  分の曲も事前コピー、Stage2は独立アドレス[STAGE2_DATA_BASE=
  0xC200]で並存」のまま。将来この変更に着手する際はStage1にも自前の
  バンク切替+RAMコピーの実装が必要(現在Stage1が自前でやっていない
  理由=単体版ROM廃止済み+`tools/verify_*.py`群のフラット64KBメモリ
  前提という制約も踏まえること、詳細はHANDOFF.md参照)。

## Round41(BGM: オクターブ下げ+デューティ比ドライバ+Stage2初期化順序
修正、続けてStage1: 全SEをchAへ統合・完了済み)(2026-09-05)

- ユーザー指示: "おｋ ではどっちの曲もオクターブ下げてくれ で、
  ドライバにデューティ比実装 6.25,12.5,25,50を実装 どちらの曲も
  パート1が25パート2が12.5 で、ステージ1から2へBGM変わる時にまだ
  ステージ2に切替わってないのにステージ2のBGMがなってるんで 初期
  描画終えてから演奏スタートな"の3件に対応。(1)オクターブ下げ:
  `tools/bgm_data/midi_to_psg.py`に`OCTAVE_SHIFT=-12`を追加、行データの
  構造は不変。(2)デューティ比: Stage2/Title/Stage1の3ファイル全ての
  BGMT_UPDATE_B/Cを「行の頭だけ音量を書く」方式から「毎tick、
  free-runningな位相カウンタをANDマスクでゲート判定」方式へ全面書き
  換え(全4種とも`1/(2^n)`のためAND1発で表現可能、除算不要)。
  パート1(chB)=25%(mask=3)/パート2(chC)=12.5%(mask=7)。新規RAM4バイト/
  チャンネル(`BGM_B/C_REST`・`BGM_B/C_PHASE`)。既存の音量ベースの
  off-by-one回帰テスト判定がデューティゲートで誤検出するようになった
  ため、判定キーをトーン周期+RESTフラグへ変更。(3)Stage2初期化順序:
  Stage1・Titleは既に「INIT冒頭でDI、末尾でEI」のブラケットを持って
  いたが、Stage2の`combined_test.asm`だけこれが無く、Stage1から
  トランポリンで飛んできた時点で割り込みが有効なまま長い初期描画を
  行っていた(Stage1の古いH.TIMIフックが、window Aの中身がStage2の
  コードに切り替わった後もそのアドレスへJPし続ける未定義動作の
  リスクがあった)。Stage1/Titleと同じブラケットをStage2にも追加し
  解消。全回帰`run_all.py` **1394 passed/0 failed**。
- 続けて実機再確認で"ステージ1の自機ショット音とBGMが被ってる 弾うつ
  とベースのほうが聞こえてない 被らないはずだぞ"という報告。調査の
  結果(`SOUND_SHOT`の12tick減衰 vs 4tick連射間隔ですきまが無くなる
  ことが直接原因)を提示したところ、ユーザーから根本的な指摘:
  **"そもそもchB、Cは空けてあってSE類はchAのみで鳴らすはず"**。
  調査の結果これは素のStage1自体の原設計(R7=0B1h、コメント
  "channel A=noise-only, B/C=tone-only")と食い違うと判明、事実関係を
  提示した上でどう進めるか確認したところユーザーから明快な回答:
  **"SEの被りで上書きされるのは問題ない BCはBGM専用 で、ノイズは
  別ch PSGは4音同時に鳴らせる トーンでノイズは消えない"**。これを
  受けて全SE(SOUND_SHOT/POD_HIT/POD_FIRE/BARRIER_HIT=トーン、
  DESTROY+エンジン音=ノイズ)をチャンネルAへ統合する大掛かりな書き
  換えを実施: R7ミキサーを`0B1h`→`0B0h`(トーンAの有効ビットを追加、
  AY-3-8910はチャンネルごとにトーン/ノイズが独立ビットのため互いを
  消し合わない)、旧`SND_TIMER_B`/`SND_TIMER_C`を`SND_TONE_TIMER`へ
  統合(音色は完全無変更)、旧`SND_C_DUTY_TIMER`を`SND_BARRIER_DUTY_
  TIMER`へ改名、`SOUND_UPDATE_B`/`SUC_NORMAL`を撤去し単一の
  `SOUND_UPDATE`が3段優先度(バリア>トーン>ノイズ)でR8を1つだけ選ぶ
  設計に統合、`BGMT_UPDATE_B/C`のSFX優先チェックを完全撤去(Stage2/
  Titleと構造的に完全同型になった)。`tools/verify_sound_duty_
  cycle.py`/`tools/verify_player_damage.py`/`tools/verify_stage1_
  bgm.py`を新設計に合わせ改訂。全ての`tools/verify_*.py`を横断
  再実行(`verify_idcache_multiframe.py`等の既知の無関係な既存問題
  以外は全PASS)。全回帰`run_all.py` **1394 passed/0 failed**、
  `verify_stage1_bgm.py` **40 passed**、`verify_sound_duty_cycle.py`
  **46 passed**、`verify_player_damage.py` **55 passed**。3ROM
  (Stage2単体・Title単体・Comb)再ビルド・`verify_comb.py`確認済み。
  詳細・技術的経緯は`tools/stage2_combined/HANDOFF.md`のRound41を
  参照。
  - **保留**: チャンネルA統合後の複数SE同時発生時の優先度・個別の
    減衰時間は未調整の初期値、実機での聞こえ方次第で再調整の可能性
    あり。

## Round41 実機フィードバック対応: Title→Stage1バンク切替フリーズの
修正(2026-09-06、完了済み)

- ユーザー報告"バンク切り替えに失敗してる タイトルでボタンを押すと
  フリーズ"に対応。根本原因: Round40のBGM(H.TIMI割り込み駆動)導入
  以降、TitleのWAIT_FOR_STARTがStage1へトランポリンする2ホップの間、
  割り込み許可のままだった - hop2完了からStage1自身がDIを実行する
  までの間にH.TIMIが発火すると、Titleの古いフックが既にStage1の
  コードに切り替わったwindow Aの中身を誤実行する未定義動作があり、
  これがフリーズの直接原因(z80emu.pyは割り込みを一切シミュレート
  しないため、既存の回帰テストでは検出不可能な種類のバグだった)。
  修正: 送り手側(Title)のhop1直前にDIを追加(受け手側のDI実行
  タイミングに依存せず競合を完全に閉じる)。同じ競合が存在した
  Stage1→Stage2切替(`build_full_rom.py`のMAINLOOP_PATCH)にも予防的に
  同じ修正を適用。`title_test.py`/`verify_comb.py`に「バンク切替直後
  IFF1=Falseであること」を直接検証する回帰テストを追加(DIを一時的に
  外して実際にFAILすることを自己検証済み)。全回帰`run_all.py`
  **1394 passed/0 failed**、`title_test.py` **42 passed**、
  `verify_comb.py`全チェックPASS。3ROM再ビルド・送付済み。詳細は
  `tools/stage2_combined/HANDOFF.md`のRound41実機フィードバック対応
  参照。
  - **教訓**: バンク切替トランポリンを新設・変更する際は、着地後
    受け手側が実際にDIするまでの間に割り込み許可のまま実行される
    命令があるかを都度確認し、送り手側で先にDIしておくことを標準の
    作法とする(z80emu.pyは割り込みを一切シミュレートしないため、
    この種の競合は回帰テストが全緑でも実機でのみ発現しうる)。
  - **(2026-09-06追記、上記は不十分と判明・下記で真因特定済み)**
    上記DI修正後もユーザーから"変わらない フリーズする...バンクは
    切り替わってたがリセットがかかってその後Stage1になっていた"との
    再現報告、続けて"そうではない WebMSXでフリーズやリセットがかかる
    これは実機も同様"(実機固有のクセではなくコード側バグと確定)。
    詳細は次項参照。

## Round41 実機フィードバック対応その2: Title→Stage1バンク切替
フリーズの真因特定・修正(2026-09-06、完了済み)

- 上記DI修正だけでは直らなかったため、このセッション内で新たに
  openMSX(headless、`-control stdio`外部制御+`reverse`逆デバッグ)
  ベースの自作デバッグ環境を構築し(`set power on`を明示しないと
  クロックが凍結したままという詰まりを解決したのが鍵)、PC=0への
  疑似リセット着地を秒単位で再現・1命令単位まで遡って特定。
  **真因1**: `CALL INIT32`(BIOSのSCREEN1初期化)自体が実機挙動として
  H.TIMI含む複数のシステムフックを無条件にRETへ巻き戻す(z80emu.py
  もこの挙動を再現しており、CALL INIT_BGMをINIT32より前に置いたら
  即座に検出できた)。**真因2**: round40より前からある古い1行
  `LD A,0C9h : LD (0FD9Fh),A`("force H.TIMI to bare RET"、当時は
  MAINLOOP突入時の一度きりのHALTを安価にする最適化だったが、BGM_TICK
  導入後は完全に有害な遺物)がINIT中盤でH.TIMIフックを無条件に踏み
  潰し続けていた。修正: `CALL INIT_BGM`を「CALL INIT32の直後、以降の
  全ローカルEIより前」へ配置し直し、上記の遺物1行を完全削除。
  **真因3(副産物、対症療法のみ実施)**: BGM_TICKが正しく常時発火する
  ようになった結果、それまで潜在化していた別バグが顕在化 - ボタン
  押下後数秒でSP(スタックポインタ)がSTACKTOP(0F380h)から異常に
  深く(0E4D8h〜さらに深い0C000h付近まで観測)落ち込むことがあり、
  `SCORE_DIGITS`(スコア6桁のスクラッチバッファ、旧0E4D8h)がその
  経路上にあったため`SCORE_DISPLAY`自身が自分の戻り先アドレスを
  上書きしてPC=0へ暴走していた。`SCORE_DIGITS`を安全な未使用領域
  (0F22Bh、STACKTOP直近の341バイトの空き)へ移設し対症。**ただし
  深いSPの根本原因自体は未解明**(自作PythonテストでBGM_TICK単体の
  PUSH/POP収支を300通りの疑似割り込みタイミングで検証した結果は常に
  完全ゼロで、少なくとも自分たちのコード側に明確な漏れは無いと
  確認済み - Stage2/Titleも同一設計だが実機・WebMSXでの多数回の
  フィードバックでこの種の自然リセットは一度も報告が無いこと、
  調査に使ったopenMSXの`C-BIOS_MSX1`が実機/WebMSXとは別実装の
  BIOSであること(この環境に実機BIOSダンプは無く比較不可)を踏まえ、
  **C-BIOS+openMSXという調査環境固有の再現である可能性を否定できて
  いない**と判断し、`SCORE_DIGITS`移設のみで一旦区切った)。全回帰
  `run_all.py` **1394 passed/0 failed**、`verify_stage1_bgm.py`
  **40 passed**。`verify_comb.py`全チェックPASS(HTIMI_HOOK再設置の
  直接アサーションが今度こそ正しく通ることを確認)。3ROM再ビルド・
  送付済み。詳細はHANDOFF.md Round41実機フィードバック対応その2参照。
  - **保留・要注意**: もし実機・WebMSXで「起動直後は遊べるが数秒〜
    数十秒後に不意にタイトルへ戻る/リセットする」症状が再現した場合、
    深いSPの根本原因調査を再開すること(この時点では実機/WebMSXでの
    再現は未確認)。

## Round41 実機フィードバック対応その3: 前回の「真因1」は誤診断だった -
CALL INIT_BGMを再度INIT32より前へ(2026-09-06、対応済み・実機再検証待ち)

- 前回の修正版ROMをユーザーが実機・WebMSX・BlueMSXで再検証した結果:
  "ダメだな 実機、WebMSX、BlueMSX全てでタイトルでボタン押下後フリーズ"、
  続けて"今回はタイトルでボタン押したら MSXの起動ロゴは出ないが
  タイトルはクリアされてフリーズだな 実機でも全く同じ挙動"。「起動
  ロゴが出ない」ため、以前観測していたPC=0への暴走ジャンプ(疑似
  リセット)とは別の、真のハングである可能性が高い。また"タイトル
  実装時点では問題なく動いてた BGMは鳴ってないROMで"との確認もあり、
  「Round39(BGM無し)は無罪、Round40のBGM導入が原因」は確定情報。
- **前回の「CALL INIT32がH.TIMIを含む複数のシステムフックを無条件に
  巻き戻す」という診断は誤りだったと判明**: `tools/z80emu.py`の
  `bios_call()`でINIT32(target==0x006F)は単なるno-op(HTIMI_HOOKは
  疎か何もしない)と直接確認。前回の観測結果は、当時まだ削除していな
  かった遺物1行(`LD A,0C9h:LD(0FD9Fh),A`)がHTIMI_HOOKを踏み潰して
  いたことだけが原因で、INIT32の位置とは無関係だった - 2つの変更
  (順序入替+遺物削除)を同時にテストした典型的な交絡変数の誤診断。
- **再考した真の懸念**: z80emu.pyのINIT32が完全なno-opということは、
  実機BIOSが画面モード初期化内部で行う「垂直帰線を待つ」処理
  (一般にEI:HALT:DIで実装)をこのテスト環境は一切検証できていない。
  この待機の瞬間にH.TIMIがTitle自身の(window Aは既にStage1に
  切り替わっている)古いアドレスを指したままだと、CALL INIT32内部で
  割り込みが発火した瞬間に暴走する - これが3プラットフォーム全てで
  100%再現する真因である可能性が高い(画面モード変更のたびに実機では
  原則毎回発生するため決定的)。
- **修正**: `CALL INIT_BGM`をCALL INIT32の前、DIの直後という最も早い
  位置へ再度戻した(遺物行の削除は維持)。全回帰`run_all.py`
  **1394 passed/0 failed**、`verify_stage1_bgm.py` **40 passed**、
  `verify_comb.py`全チェックPASS(ただしz80emu.py環境ではこの種の差を
  検出できないため、テストが通ること自体は正しさの保証にならない)。
  Comb ROM再ビルド・送付済み。詳細はHANDOFF.md Round41実機フィード
  バック対応その3参照。
  - **保留・要実機再検証**: この修正が実際にフリーズを解消するかは
    実機・WebMSX・BlueMSXでの再検証待ち(このセッションのテスト環境
    では原理的に検証不可能)。
  - **教訓(重大)**: `tools/z80emu.py`は多数のBIOSルーチン(INIT32/
    WRTVDP/LDIRVM等)を「即座に副作用なくreturnする」単純なスタブとして
    実装しており、「実BIOSルーチン内部で何が起きているか」に依存する
    バグは原理的に検出・再現できない。「テスト全緑なのに実機/複数の
    独立エミュレータで100%再現するフリーズ」に遭遇したら、z80emu.pyが
    no-op化しているBIOSルーチンの実際の内部動作を疑うこと。また1回の
    テストサイクルで2つ以上の変更を同時に加えないこと(交絡変数の
    誤診断を招く)。

## Round41 実機フィードバック対応その4: ユーザー仮説を受けた追加の防御策
(2026-09-06、対応済み・実機再検証待ち)

- ユーザー仮説: "なのでサウンドドライバというかVsyncでの実装がダメって
  ことだな もしかするとボタン押すって事はBIOS経由してるんで サウンド
  ドライバが破壊されてるかもな"(GTTRIGがBIOS経由である点への着目)。
  裏付けとなる具体的欠陥は特定できなかったが(GTSTCK/GTTRIGはページ0
  内の単純呼び出しでスロット切替を伴わない)、DIより前の未知の経路での
  H.TIMI発火を排除できない以上、多重防御として`title_test.asm`の
  WAIT_FOR_STARTのDI直前にH.TIMIフックを明示的にbare RETへ戻す1行を
  追加。`title_test.py`42件・`verify_comb.py`・全回帰1394件、いずれも
  PASS。Title単体ROM・Comb ROM再ビルド・送付済み。詳細はHANDOFF.md
  Round41実機フィードバック対応その4参照。
  - **重要**: これも含め一連の修正は全て「z80emu.py/openMSXでは検証
    できない実機固有の挙動」への対症的な防御策の積み重ねであり、根本
    原因を完全特定できているわけではない。もし今回も実機・WebMSX・
    BlueMSXでフリーズが再現する場合、次の切り分けとして「Titleが
    最初からH.TIMIフックを一切設置しない(音楽無し)診断用ROM」を作成し
    BGM/H.TIMI機構そのものが原因かを確認する案を検討する(未実施)。

## Round42: Stage1 SEショット優先実装 + BGM HWエンベロープジェネレータへ
全面置換(2026-09-06、完了済み・実機フィードバック待ち)

- ユーザー最終決定: "色々考慮しての帰結として サウンドの振り分けは
  BGMのB,Cと自機ショットAchで分けたうえでSEをAに統合するのは同じだが
  SE優先でショットは消す仕様に SE発声中はショット音は鳴らないと で
  BGMだが取り敢えずHWエンベロープ5番で ダメならソフトウェアに変える
  が、今のデューティ比はおかしい 50%は基本の矩形波で音は変わらない
  はずだが断続音になってる デューティ比は元から50%"。
- **Stage1 SEショット優先**: 新規1バイトフラグ`SND_TONE_IS_SE`
  (0F231h)を追加。`SOUND_POD_HIT`/`SOUND_POD_FIRE`が発火時に1を立て、
  `SOUND_SHOT`は「共有タイマー`SND_TONE_TIMER`が非0かつこのフラグが
  非0」の間だけ発射要求自体を握りつぶす(SEの減衰を妨げない)。
- **BGM: ソフトウェアデューティゲート→HWエンベロープジェネレータ**:
  AY-3-8910のトーンは常に固定50%デューティのため、音量ON/OFFで
  「デューティ比」を模したソフトウェア実装(round41で導入)は原理的に
  誤りだった(ユーザー指摘通り)。R11-13(エンベロープ周期・形状)+
  R8-10のbit4(共有エンベロープ使用フラグ)という本来のハードウェア
  機能に置き換え、形状は#5(R13=8h、繰り返し減衰のこぎり波)を選択。
  AY-3-8910のエンベロープジェネレータはチップ全体で1個しか無い共有
  リソースのため、chB(パート1)が音符の頭でのみR11-13をリトリガーする
  "駆動側"、chC(パート2)はR10のbit4だけ立てて追従する"追従側"という
  非対称設計を自己決定(ユーザー未確認、実機フィードバック次第で
  見直しの可能性あり)。ユーザー自身の警告("毎フレームPSGに書き込むと
  HWがアタックだけ繰り返される")を踏まえ、行の継続tick中はPSGへ
  一切書き込まない(リトリガー回避)設計を徹底。Stage1/Stage2/Title
  の3ファイル全てに同一パターンで適用。
- 全回帰`run_all.py` **1398 passed/0 failed**(1394→1398)。
  `verify_sound_duty_cycle.py`(49)/`bgm_test.py`(82)/`title_test.py`
  (42)/`verify_stage1_bgm.py`(41)/`verify_player_damage.py`(55)全て
  PASS。3ROM(Stage2単体・Title単体・Comb)再ビルド・`verify_comb.py`
  健全性確認の上、送付済み。詳細・技術的経緯・自己発見バグは
  `tools/stage2_combined/HANDOFF.md`のRound42を参照。
- **保留・実機フィードバック待ち**: HWエンベロープの形状/周期は
  「取り敢えず」の初期選択(「ダメならソフトウェアに変える」という
  撤退条件が既に与えられている)。chB駆動/chC追従の非対称設計は
  ユーザー未確認。SEショット優先の実際の聞こえ方も実機フィードバック
  待ち。

## Round42 実機フィードバック対応: SEショット優先の実バグ修正+BGM
エンベロープ形状#5→#1(2026-09-06、完了済み・実機フィードバック待ち)

- ユーザー報告: "まずSEがほぼ鳴らずショット音が残ると言う逆になってる
  でHWエンベロープも期待した音になってない テストプログラムと全く
  違ったがサウンド なので一番無難な1番に変更 それでダメならソフト
  ウェアにする そうじゃないとそっちで音のコントロールが出来ない
  お前は音楽は分からないからな"。
- **実バグ発見・修正**: `SOUND_SHOT`のSE優先ゲートが`SND_TONE_TIMER`/
  `SND_TONE_IS_SE`(POD_HIT/POD_FIRE用)しか見ておらず、独立タイマー
  `SND_BARRIER_DUTY_TIMER`で管理される`SOUND_BARRIER_HIT`(3種のSE中
  最優先)を見落としていた。バリアヒットも同じR0/R1(トーン周期)を
  書くため、バリア減衰中でもショットがR0/R1を横取りし、「バリアの
  音量リズムでショットの音程が鳴る」壊れた合成音になっていた -
  これが「SEが鳴らずショットが残る」の実体。`SOUND_SHOT`に
  `SND_BARRIER_DUTY_TIMER!=0`チェックを追加して解消、新規回帰テスト
  5件(修正の一時取り消しでFAILすることも自己検証済み)。
- **BGMエンベロープ形状**: #5(繰り返し減衰のこぎり波、8h)が実機で
  試聴ツールと「全く違った」サウンドだったため、指示通り#1(一度だけ
  減衰して0で停止、9h)へ変更(Stage1/Stage2/Title全て、EP=600は
  無変更)。#1は音符の長さがエンベロープ周期より長いと早期に無音化
  する性質があることをユーザーへ提示済み(指示通り周期調整はせず
  形状のみ変更)。それでもダメなら「ソフトウェアに変える」という
  撤退条件は引き続き有効。
- 全回帰`run_all.py` **1398 passed/0 failed**(無変化)、
  `verify_sound_duty_cycle.py` **54 passed**(49→54)、その他BGM関連
  テスト全てPASS。3ROM再ビルド・`verify_comb.py`健全性確認の上、
  送付済み。詳細はHANDOFF.mdのRound42実機フィードバック対応参照。
- **保留・実機フィードバック待ち**: エンベロープ#1の実際の聞こえ方
  (特に長い音符の早期無音化)・SEショット優先修正後の聞こえ方、
  いずれも実機確認待ち。

## Round43: BGM HWエンベロープを完全撤回、ソフトウェアエンベロープへ
全面移行(2026-09-06、完了済み・実機フィードバック待ち)

- ユーザー報告(エンベロープ#1へ変更後の実機確認): "BGMが1chしか
  なってないと言うか 恐らくエンベロープの影響で発音できてないな
  HWエンベロープはコントロール不能と判断 ソフトに切り替える まずは
  普通にピアノ減衷で てか作ったツールで決めるんで 8種類ソフト
  エンベロープで作ってくれ これならデューティ比にも対応できるからな"。
- **根本原因確定**: AY-3-8910のエンベロープジェネレータはチップ全体で
  1個しか無い共有リソースであり、独立タイミングで進行する2音楽
  チャンネル(chB/chC)を1つの共有ジェネレータで駆動しようとした
  Round41由来の「chB駆動/chC追従」設計自体が構造的に破綻していたと
  実機テスト(shape#5・#1いずれも失敗)で確定。HWエンベロープを完全
  撤回し、各チャンネルが完全独立に音量を計算するソフトウェア方式へ
  全面移行。
- 試聴ツール(PSG BGM Bench)を8種ソフトエンベロープ(PIANO/PLUCK/
  BELL/ORGAN/LINEAR/STACCATO/SWELL/HARPSICHORD)+Duty overlay機能へ
  全面改訂、ユーザーの最終選定: **"おｋ ではBGM パート1はBellで
  デューティ比50 パート2はリニアデューティ比はOFFで"** -
  **パート1(chB)=BELL+デューティ比50%、パート2(chC)=LINEAR+デュー
  ティ無し**に確定。
- 各チャンネル専用RAM状態(音量レベル・テーブルindex・カウントダウン)
  +RLE圧縮(level,duration)テーブル(ROM上、BELL/LINEAR各16エントリ
  32バイト、終端は(0,0)の「無期限保持」番兵)を歩く方式で実装。
  デューティ50%は独立位相カウンタによるオーバーレイとして任意の
  エンベロープ形状と自由に組み合わせ可能(共有ハードウェア資源が
  不要になったため、休符でない限り毎tick無条件にR9/R10へ書く設計に
  変更、HWエンベロープ特有のリトリガー回避制約はもう存在しない)。
  Stage1/Stage2/Title全3ファイルの`BGMT_UPDATE_B/C`を全面書き換え、
  旧`BGM_ENV_SHAPE`/`BGM_ENV_PERIOD_LO/HI`/`BGM_VOL_ENV`は削除。
  ROMコストは各ファイル+256バイト。
- 新規回帰テスト`bgm_test.py`(81)/`title_test.py`(39)/`verify_
  stage1_bgm.py`(40)全て全面書き換え、多tick分の実出力列をPython
  リファレンス(`sim_envelope_sequence`)と直接突き合わせ番兵到達まで
  検証。全回帰`run_all.py` **1397 passed/0 failed**。3ROM再ビルド・
  `verify_comb.py`健全性確認の上、標準方針によりComb ROMのみ送付。
  詳細はHANDOFF.mdのRound43参照。
- **保留・実機フィードバック待ち**: BELL(chB+50%duty)/LINEAR(chC)の
  実際の聞こえ方は実機フィードバック待ち(z80emu.pyには音声シミュ
  レーションが無いため、この種の判断は実機/試聴ツールでしか確認
  できない)。

## Round44: BGM音量調整(一律-4減衰・完了済み・実機フィードバック待ち)
(2026-09-06)

- ユーザー: "OK ではBGM音量を下げたいが 現在は最大か?" → 回答:
  BELL/LINEARどちらもピーク(発音直後)がレベル15、AY-3-8910の4bit
  音量レジスタの最大値そのもの。AskUserQuestionで下げ方(試聴ツール
  で決める/具体的な幅を直接指定)→後者、続けて幅(-2/-4/-6)を確認 →
  **「中程度下げる(-4、ピーク11)」**に確定。
- テーブル自体は変更せず、R9/R10へ書く直前に新規`BGM_VOL_ATTEN EQU 4`
  を一律減算(アンダーフロー時は0クランプ)。Stage1/Stage2/Title全3
  ファイルへ同一実装。RAM上のエンベロープ状態は非減衰のまま、減衰は
  書き込み時のみ - 将来幅を変える際もEQU一箇所の変更で済む設計。
  ROMコストは各+10バイト。
- 回帰テスト(`bgm_test.py`/`title_test.py`/`verify_stage1_bgm.py`)を
  更新、Stage1のchB側の減算を一時無効化し実際に3件FAILすることを
  自己検証した上で復元。全回帰`run_all.py` **1397 passed/0 failed**。
  3ROM再ビルド・`verify_comb.py`健全性確認の上、標準方針によりComb
  ROMのみ送付。詳細はHANDOFF.mdのRound44参照。
- **保留**: -4という下げ幅が実機で適切かは実機フィードバック待ち。
  以後の調整は`BGM_VOL_ATTEN`のEQU値変更のみで対応可能。

## Round45: Stage1 BGMテンポ微増+タイトル画面をSC2アートへ差し替え・
RLE圧縮実装(2026-09-06、完了済み・実機フィードバック待ち)

- ユーザー: "ではStage1のBGMのテンポを少し速く で添付ファイルは
  スクリーン2用のSC2ファイル これをタイトル画面に変更 但し簡単な圧縮を
  かけてくれ MSXでも使える程度のデコードが軽い物 どれくらい縮んだか
  教えてくれ"(添付Title.SC2)。
- **BGMテンポ**: `midi_to_psg.py`にALONE_FIGHTER専用のテンポ倍率
  (`TEMPO_SCALE`、1.1倍)を追加、MIDI→vblank tick変換係数を底上げし
  総再生時間を約10%短縮(DEFEAT/Stage2は無変更)。
- **タイトル画面**: round39の"適当な"SCREEN1プレースホルダー(Stage1/
  Stage2ボス+PUSH STARTテキスト)を廃止し、ユーザー提供のSCREEN2
  アート(`tools/title_screen/assets/Title.SC2`)へ全面差し替え。
  `CALL INIT32`→`CALL INIGRP`でSCREEN2モードへ切替(VRAMベース
  アドレスはSCREEN1/2共通のため既存EQU定数はそのまま流用可能)。
- **自前RLE圧縮**(`tools/title_screen/title_bg_gen.py`新設): 制御
  バイトbit7でリテラル/反復を切り替える対称フォーマット、終端は
  ストリームに埋め込まずセグメント数を16bit down-counterとして
  持つ設計。Z80デコーダはVDPアドレスのオートインクリメントを活かし
  1回のCALLで丸ごと展開(`OTIR`でリテラルを一括転送、`DJNZ`で反復)。
  **実測圧縮率: 14336→11767バイト(82.1%、約18%削減、2569バイト
  節約)** - パターンジェネレータは77.8%まで縮むが、カラーテーブルは
  ほぼ無圧縮(100.3%、8px単位で色が細かく変わる精密なドット絵の
  エントロピーそのもので、アルゴリズムの欠陥ではないと連長分布の
  実測で確認済み)。
  展開直後にスプライト属性テーブル先頭を0D1h(全停止)へ明示上書き
  (このタイトルバンクはスプライトパターンを持たないため安全策)。
  `z80emu.py`にINIGRP(0072h)のno-opインターセプトを追加。
  レンダリングで視覚確認(「DOUBLE MISSION ARCADE CLASSIC」ロゴが
  正しく表示)。新規回帰テスト(展開VRAM完全一致・セグメント数
  一致・Pythonラウンドトリップ)を追加、Z80デコーダの反復長算出を
  一時破壊して実際にFAILすることも自己検証済み。
- 全回帰`run_all.py` **1397 passed/0 failed**。3ROM再ビルド・
  `verify_comb.py`健全性確認の上、標準方針によりComb ROMのみ送付。
  詳細はHANDOFF.mdのRound45参照。
- **保留**: BGMテンポ+10%・タイトル画面の見え方いずれも実機
  フィードバック待ち。圧縮率をさらに上げるにはLZ系アルゴリズムへの
  変更が必要(「簡単で軽いデコード」という要件から今回は対象外)。

## Round46: タイトル画面アート差し替え(TDM.SC2・完了済み・実機
フィードバック待ち)(2026-09-06)

- ユーザー: "ではタイトルをこっちに差し替えて"(添付TDM.SC2)。
  Round45で構築したパイプライン(`tools/title_screen/assets/Title.SC2`
  を差し替え→`title_bg_gen.py`再実行)がそのまま機能し、ASM・テスト
  側の変更は一切不要。
- 新アートは圧縮率がさらに向上: **14336→9646バイト(67.3%、約33%
  削減、4690バイト節約)**(前回18%削減より改善、色変化が緩やかな
  領域が多くカラーテーブルの圧縮が効いたため)。Title単体ROM使用量
  12472→10351バイトへ縮小。
- レンダリングで視覚確認(ロゴ+宇宙船+ロボットのアート)。全回帰・
  `title_test.py`(25件、無改修で全PASS)・`verify_comb.py`確認済み。
  3ROM再ビルド・標準方針によりComb ROMのみ送付。詳細はHANDOFF.mdの
  Round46参照。
